% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{triage_group_slice}
\alias{triage_group_slice}
\title{Slice Data to Every Row One Group's Panels Could Need}
\usage{
triage_group_slice(data, grp)
}
\arguments{
\item{data}{The \code{literature_analysis_ready} target.}

\item{grp}{A one-row tibble of group-defining columns.}
}
\value{
A filtered data frame, a superset of every subset the panels derive.
}
\description{
A minimal standalone input for \code{\link[=write_triage_plots_for_group]{write_triage_plots_for_group()}}, so a crew
worker can be sent one group's rows instead of the whole 90,110-row table.
}
\details{
It matches on \code{\link[=triage_group_cols]{triage_group_cols()}} minus \code{\link[=triage_relaxed_cols]{triage_relaxed_cols()}}, which makes
it a \strong{superset} of all three subsets the plotting function derives. That
superset property is the whole point: \code{filter_to_group()} still runs inside
the plotting function unchanged, and filtering a superset with the same
predicate returns exactly the same rows as filtering the full table. Output is
therefore byte-identical, and no plot function needed changing.

Filtering to the \emph{strict} group instead would be wrong and would not error:
panels (a) and (d) would render with a single unit and a single site type and
still look plausible.
}
