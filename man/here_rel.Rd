% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_paths.R
\name{here_rel}
\alias{here_rel}
\title{Project-Relative Path}
\usage{
here_rel(...)
}
\arguments{
\item{...}{Path components, passed to \code{\link[here:here]{here::here()}}.}
}
\value{
A character vector of paths relative to the working directory.
}
\description{
\code{\link[here:here]{here::here()}} with the answer expressed relative to the working directory,
so it is safe to store in \code{tar_meta} or hand to a \code{format = "file"} target.
}
\details{
Anchoring is unchanged: resolution still goes through here, so
\code{here::i_am("Readme.md")} in \verb{_targets.R} still defines the root. Only the
representation differs.

Relative to \emph{the working directory}, which under \code{tar_make()} is the project
root. That is also the right answer when the working directory is somewhere
else: called during a Quarto render rooted in \verb{docs/}, \code{here_rel("data/x")}
gives \code{"../data/x"}, which resolves correctly from there.

Returns a plain character vector, not the \code{fs_path} that \code{\link[fs:path_rel]{fs::path_rel()}}
hands back. targets accepts \code{fs_path} (it inherits from character) but hashes
it differently, so a bare character keeps target invalidation predictable.
}
\examples{
\dontrun{
here_rel("data/clean/decisions/group_decisions.csv")
#> "data/clean/decisions/group_decisions.csv"      (not "C:/Users/.../data/clean/...")
}
}
