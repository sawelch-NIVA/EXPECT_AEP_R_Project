% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_edges.R
\name{node_card_extent}
\alias{node_card_extent}
\title{Half-Extent of a Node Card, in Data Units}
\usage{
node_card_extent(
  nodes,
  image_size = 0.19,
  card_aspect = 1.8/2.4,
  device_aspect = 12/8,
  x_expand = 0.15,
  y_expand = 0.12,
  x_range = NULL,
  y_range = NULL
)
}
\arguments{
\item{nodes}{The nodes table, with \code{x} and \code{y}. Ignored for an axis where
\code{x_range}/\code{y_range} is supplied.}

\item{image_size}{Card width as a fraction of panel width, as passed to
\code{ggimage::geom_image()}.}

\item{card_aspect}{Card height divided by card width, in inches.}

\item{device_aspect}{Device width divided by device height, in inches.}

\item{x_expand, y_expand}{The multiplicative axis expansions \code{\link[=plot_aep]{plot_aep()}} uses.}

\item{x_range, y_range}{Raw (pre-expansion) \code{c(min, max)} to use instead of
\code{range(nodes$x)}/\code{range(nodes$y)}. \code{NULL} (the default) derives from
\code{nodes}, exactly as before this parameter existed.

\strong{Added 2026-08-08, and it is the fix for a real bug, not a nicety.}
\code{nodes} alone is only correct if the FINAL rendered panel's data range
equals the node coordinate range expanded by \code{x_expand}/\code{y_expand} -- but
\code{\link[=plot_aep]{plot_aep()}} also draws group boxes (\code{aep_group_layers()}), which are
real plotted data (\code{geom_rect()}) that auto-expand the panel range
whenever a box's padding pushes past what \code{x_expand}/\code{y_expand} alone
would add. When that happens, \code{hw}/\code{hh} come out smaller than the panel
actually is. That is silent for \code{ggimage::geom_image()}, whose size is a
fraction of PANEL WIDTH and does not care what the data range is -- but
it directly undersizes anything sized in DATA units against the same
\code{hw}/\code{hh}, which by 2026-08-07 included \code{geom_tile()} for bare nodes and
the edge-clipping box both. Sam measured it directly: a bare node's
rendered rect came out 251x162px against the real card's 310x232px, and
162/251 = 0.645 while the real card's own aspect is 0.75 -- not just
smaller, skewed, because the node x-range and y-range differ in how much
the group box's padding exceeds \code{x_expand}/\code{y_expand}'s guess on each
axis. \code{plot_aep()} now computes the TRUE combined range (nodes + boxes)
and passes it in here, then pins the actual rendered panel to that exact
window with \code{coord_cartesian(expand = FALSE)}, so this function's
prediction and the real render can no longer disagree.}
}
\value{
A list with \code{hw}, \code{hh} (half-width/height in data units) and \code{rx},
\code{ry} (the full expanded span each was computed from, for callers that
need to reconstruct the axis window, e.g. \code{\link[=plot_aep]{plot_aep()}}).
}
\description{
The geometry \code{\link[=aep_edge_coords]{aep_edge_coords()}} needs in order to stop an arrow at the edge
of a card rather than under it.
}
\details{
\code{ggimage::geom_image()} sizes an image as a fraction of \strong{panel width}, so
its extent in data units depends on three things this function has to be told:
the coordinate range (after the expansion \code{\link[=plot_aep]{plot_aep()}} applies), the shape of
the card image itself, and the shape of the output device. Getting any of them
wrong moves the clip boundary, which is why they are named arguments with
documented defaults rather than constants buried in the drawing code.

Defaults match the \code{aep_diagram} target: \code{node_cards_compact} is written at
2.4 x 1.8 inches (\code{card_aspect = 1.8 / 2.4}) onto a 12 x 8 inch canvas
(\code{device_aspect = 12 / 8}). \strong{Change either in \verb{_targets.R} and these defaults
are wrong}, so both are passed explicitly from there.
}
