% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_sample_groups_table.R
\name{build_sample_groups_table}
\alias{build_sample_groups_table}
\title{Build the Sample-Groups Display Table}
\usage{
build_sample_groups_table(summary_data, ids = NULL)
}
\arguments{
\item{summary_data}{The \code{summarise_literature_data} target.}

\item{ids}{The \code{group_ids} ledger, or \code{NULL}. When supplied, the table's
first column shows \code{\link[=format_composite_group_id]{format_composite_group_id()}}'s composite label
(e.g. \code{"G006-Bf-Cnr-G.mor-Liv-Mw"}) rather than the bare \code{group_id}.
This is DISPLAY ONLY: \verb{type into group_decisions.csv} still means the
bare \code{group_id} (e.g. \code{"G006"}) -- that value, not the composite label,
is what \code{lump_into} cells, \code{aep_nodes.csv} notes and every
\verb{\{#grp-G006\}} heading anchor in \verb{docs/groups/*.qmd} actually point at,
and none of those are touched here. A group with no compartment or
geography code yet (see \code{misc-todo.md} item 13) falls back to its bare
\code{group_id} in this column too.}
}
\value{
A tibble sorted by \code{n} descending, with columns \code{group_id}, \code{group},
\code{location}, \code{dates}, \code{n}, \code{mean_sd}, \code{median}, \code{n_outliers},
\code{dip_p_label}, \code{n_units}, \code{dropped_label}, \code{.is_multimodal}, \code{.is_outlier},
\code{.anchor}.
}
\description{
Reshapes \code{summarise_literature_data} into the columns actually shown to a
reader: compartment/taxonomy folded into one \code{group} label, geography folded
into \code{location}, and mean/SD/unit folded into a single cell.
}
\details{
Column names here are plain and code-friendly. Display labels are applied
separately by \code{\link[=sample_groups_flextable]{sample_groups_flextable()}}, so renaming a header never
requires touching this function (and vice versa).

The two \code{.}-prefixed flag columns drive conditional formatting downstream.
They are carried through rather than recomputed so that any filtering a
document applies (e.g. the manuscript showing only large groups) stays
consistent with the highlighting.

\code{.anchor} is computed for every row, but nothing is linked unless a document
asks: see the \code{link_sections} argument of \code{\link[=sample_groups_flextable]{sample_groups_flextable()}}. The
anchors only resolve in the triage notebook, and the manuscript reads this
same target, so linking by default would put dead anchors into \code{index.qmd} and
from there into the docx.
}
