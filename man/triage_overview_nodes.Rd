% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_triage_overview.R
\name{triage_overview_nodes}
\alias{triage_overview_nodes}
\title{Parent Nodes Worth an Overview}
\usage{
triage_overview_nodes(data, min_n = 100, groups = NULL)
}
\arguments{
\item{data}{The \code{literature_analysis_ready} target. Must carry \code{MEASURED_N}.}

\item{min_n}{Minimum measurements for a node to qualify.}

\item{groups}{The \code{triage_pilot_groups} target. Nodes with no displayed group
beneath them are dropped; see \code{\link[=filter_reachable_nodes]{filter_reachable_nodes()}}. \code{NULL} skips the
check.}
}
\value{
A tibble of node columns plus \code{n}, \code{n_rows}, \code{level_1}, \code{level_2}
(\code{NA} where only one level varies), \code{node_label} and \code{node_slug}.
}
\description{
One row per compartment x sub-compartment x unit carrying at least \code{min_n}
measurements, with the levels to compare resolved per node by
\code{\link[=triage_overview_levels]{triage_overview_levels()}}.
}
\details{
\code{min_n} is \code{sum(MEASURED_N)}, the \strong{same currency} as the \code{min_n} of
\code{\link[=sample_triage_groups]{sample_triage_groups()}} and the \code{n} of \code{summarise_literature_data}, and it
defaults to the same 100. That alignment is the point: an overview whose
sub-compartment contains no group large enough to appear in the notebook is
written and then never displayed, because the notebook only opens headings for
groups it is showing. Counting rows instead let those two thresholds disagree
and produced exactly that orphan (Brackish/Transitional Water: 48 measurements
across 48 rows, over a 30-row bar and under a 100-measurement one).

\code{n_rows} is reported alongside because the panels draw one mark per \emph{row}, so a
node can clear \code{min_n} on aggregated measurements while having few rows to
plot; same caveat as \code{\link[=sample_triage_groups]{sample_triage_groups()}}.

Nodes where no candidate column varies are dropped: there is nothing to
compare, and the per-group panels already cover them.
}
