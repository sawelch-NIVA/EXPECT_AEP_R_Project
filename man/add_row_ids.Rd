% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_row_ids.R
\name{add_row_ids}
\alias{add_row_ids}
\title{Add a Stable Row Identifier}
\usage{
add_row_ids(
  data,
  id_col = "SAMPLE_ID",
  disambiguate_by = row_id_disambiguators(),
  col = "row_id"
)
}
\arguments{
\item{data}{A measurements table.}

\item{id_col}{Name of the column to build from.}

\item{disambiguate_by}{Columns tried, in order, to break ties.}

\item{col}{Name of the column to add.}
}
\value{
\code{data} with \code{col} added as its first column.
}
\description{
\code{row_id} is \code{SAMPLE_ID}, with \code{\link[=row_id_disambiguators]{row_id_disambiguators()}} appended only for
those rows whose \code{SAMPLE_ID} is shared with another row.
}
\details{
\strong{The one wrinkle, stated plainly.} Because disambiguation is conditional, a
row's id can change if a \emph{new} row later arrives sharing its \code{SAMPLE_ID}:
both then gain a suffix. Appending unconditionally would remove that, at the
cost of a redundant suffix on all 90,000 rows to defend against a case that
currently affects 18. The conditional form is chosen because the failure is
\strong{loud rather than silent}: anything storing a \code{row_id} is expected to store
the selector that found it too, and the two disagreeing is an error the
pipeline raises. A positional scheme has no equivalent check, which is the
whole reason it was rejected.

Errors rather than degrading in three cases, all of which mean a stored
\code{row_id} could point somewhere unintended:
\itemize{
\item \code{id_col} absent.
\item Any \code{NA} in \code{id_col}, since there is no basis to build from.
\item Ids still colliding after every disambiguator is applied. Suffixing those
with a counter would reintroduce positional ids by the back door, so the
duplicates are reported and the build stops.
}
}
