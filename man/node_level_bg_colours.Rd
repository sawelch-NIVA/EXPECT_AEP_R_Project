% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_node_cards.R
\name{node_level_bg_colours}
\alias{node_level_bg_colours}
\title{Pastel Background Colour per Node Level}
\usage{
node_level_bg_colours()
}
\value{
A named character vector, level to hex colour.
}
\description{
Sam 2026-08-07: colour the card background by node type so the AEP reads at
a glance without following edges. Pale/pastel throughout, because these sit
behind data (text, violins, threshold lines) that has to stay legible on
top, and a saturated fill would fight it.
}
\details{
Keyed on \code{level} (\code{aep_node_levels()}), not \code{node_type}: \code{node_type} is only
\code{empirical}/\code{external}, an internal distinction about where the numbers came
from, not the source/medium/organism/target-site distinction the colouring
is meant to carry. Sam specified three: orange for \code{source}, blue for
\code{medium} (the "Key Exposure State" compartment nodes), pink for \code{organism}.
\code{tse} (target site exposure) wasn't in that list; given a fourth pastel in
the same family (lavender) rather than left uncoloured, so every node gets a
background and the palette stays internally consistent.
}
