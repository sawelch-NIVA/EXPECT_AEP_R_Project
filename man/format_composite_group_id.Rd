% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_ids.R
\name{format_composite_group_id}
\alias{format_composite_group_id}
\title{Composite Group Codes}
\usage{
format_composite_group_id(
  data,
  compartment_codes = NULL,
  subcompartment_codes = NULL,
  species_group_codes = NULL,
  geography_codes = NULL,
  geography_sub_codes = NULL,
  tissue_codes = NULL,
  unit_codes = NULL,
  species_overrides = NULL
)
}
\arguments{
\item{data}{A table carrying \code{group_id} plus at least
\code{ENVIRON_COMPARTMENT}, \code{ENVIRON_COMPARTMENT_SUB}, \code{SPECIES_GROUP},
\code{SITE_GEOGRAPHIC_FEATURE}, \code{SITE_GEOGRAPHIC_FEATURE_SUB},
\code{SAMPLE_SPECIES}, \code{SAMPLE_TISSUE}, \code{MEASURED_UNIT_STANDARD}.}

\item{compartment_codes, subcompartment_codes, species_group_codes, }{geography_codes,geography_sub_codes,tissue_codes,unit_codes The seven
lookups, from \code{\link[=read_group_code_lookup]{read_group_code_lookup()}}. \code{NULL} reads them from their
default paths in \verb{data/clean/lookups/}.}

\item{species_overrides}{Passed to \code{\link[=format_species_code]{format_species_code()}}.}
}
\value{
A character vector the same length as \code{nrow(data)}. A row with no
compartment or geography code yet warns once and falls back to the bare
\code{group_id}, so a lookup that has fallen behind the data fails loud
rather than emitting a silently wrong code. A row with a species but no
tissue code yet warns separately and drops just the species/tissue
segment, keeping the rest of the ID.
}
\description{
A human-referenceable label layered onto the stable G-number rather than
replacing it: \code{group_id} (e.g. \code{"G014"}) is already what \code{lump_into} cells
and free-text notes across the project point at (see \code{\link[=allocate_group_ids]{allocate_group_ids()}}
above for why that value can never change). This produces a second,
DERIVED label -- \code{"G014-Wfw-Cwb-G.mor-Liv-Mw"} -- for places that want the
compartment, geography, species/tissue and unit visible at a glance,
recomputed from the lookups on every call and never stored as identity.
}
\details{
Scheme (Sam 2026-08-07): \verb{G<num>-<compartment block>-<geography block> [-<species code>-<tissue code>]-<unit code>}. The compartment and
geography blocks are each a \code{\link[=group_code_block]{group_code_block()}}: one capital parent
letter directly followed by a lowercase child code, no internal hyphen, so
case alone marks the parent/child split within that block. The
compartment block's capital is Water/Earth/Air/Biota; its lowercase part
is a 2-letter sub-compartment code, or a 1-letter species-group code when
the compartment is Biota (Biota's own \code{ENVIRON_COMPARTMENT_SUB} is always
just "Biota, Aquatic" / "Biota, Terrestrial" and carries no information
the species group doesn't already give more usefully, hence the swap
rather than a fourth lookup). The geography block's capital is
\code{SITE_GEOGRAPHIC_FEATURE}'s first letter and its lowercase part is a
2-letter \code{SITE_GEOGRAPHIC_FEATURE_SUB} code.

\code{SITE_GEOGRAPHIC_FEATURE}'s own letters deliberately avoid W/E/A/B, the
compartment letters: the geography block sits immediately after the
compartment block, so reusing one of those (WWTP -> "W" would have been
the obvious pick) would put two same-lettered-but-different-meaning
capitals back to back, e.g. an aquifer sample would misleadingly read as
"GW" repeated. \code{data/clean/lookups/group_geography_codes.csv} documents
the choice per feature.

The species/tissue segment is OPTIONAL and omitted entirely for a group
with no \code{SAMPLE_SPECIES} (Sam: "we don't need to include it in stuff
without a species") -- unlike the compartment/geography blocks, its
absence is not a gap and does not warn. It is two hyphen-joined pieces
rather than one \code{\link[=group_code_block]{group_code_block()}}, because species and tissue are
peers describing the same sample rather than a parent/child pair, and the
species code already uses its own internal \code{.} (see
\code{\link[=format_species_code]{format_species_code()}}).

The unit code is always present: \code{"C"} (concentration, \code{mg/L}), \code{"Md"}
(mass, dry weight), \code{"Mw"} (mass, wet weight), or \code{"X"} for anything else
-- a real category, not a gap, so an unmapped unit falls back to \code{"X"}
silently rather than warning.
}
