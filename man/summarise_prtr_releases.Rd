% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_prtr_emissions.R
\name{summarise_prtr_releases}
\alias{summarise_prtr_releases}
\title{Aggregate PRTR Releases}
\usage{
summarise_prtr_releases(d, by, drop_incomplete = TRUE, frac = 0.25)
}
\arguments{
\item{d}{Output of \code{\link[=read_prtr_long]{read_prtr_long()}}, optionally pre-filtered.}

\item{by}{Character vector of grouping columns.}

\item{drop_incomplete}{Exclude years flagged by \code{\link[=prtr_complete_years]{prtr_complete_years()}}?
\code{TRUE} by default, because including them is wrong rather than merely
cautious. See that function.}

\item{frac}{Passed to \code{\link[=prtr_complete_years]{prtr_complete_years()}}.}
}
\value{
A tibble, one row per group, ordered by \code{total_kg_yr}. \code{n_dropped} is
how many years were excluded as incomplete.
}
\description{
\strong{Two different numbers, and confusing them is the easiest mistake here.}
}
\details{
\itemize{
\item \code{total_kg_yr} -- sum across facilities within a year, then averaged over
years. This is the annual release of the source, and it is what an AEP
source node's magnitude should be.
\item \code{mean_kg_yr} -- mean of individual facility-year values. Describes a typical
facility, not the sector.
}

The first version of this reported only the second and labelled it the sector
release. For land-based industry to water that is 149 kg/yr against a true
national total of 12,848: a factor of 25, in the direction that makes a source
look negligible.

\code{sd_total_kg_yr} is the spread of the ANNUAL TOTALS, so it describes year to
year variation in the sector, which is the uncertainty an external node wants
beside its magnitude. \code{sd_kg_yr} is the spread between facility-years and is
much larger, because facility sizes span orders of magnitude.
}
