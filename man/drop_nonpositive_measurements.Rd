% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_analysis_ready.R
\name{drop_nonpositive_measurements}
\alias{drop_nonpositive_measurements}
\title{Drop Non-Positive and Missing Measured Values}
\usage{
drop_nonpositive_measurements(data, value_col = "MEASURED_VALUE_STANDARD")
}
\arguments{
\item{data}{A data frame with a measured value column.}

\item{value_col}{Name (string) of the measured value column to filter on.}
}
\value{
\code{data} with offending rows removed.
}
\description{
Removes rows whose measured value is \code{NA}, zero, or negative. This is the
only data-hygiene step applied before analysis; nothing more exotic (no
censored-data modelling, no LOD reconstruction) is attempted.
}
\details{
Rationale for each case:
\itemize{
\item \code{NA} — no measurement to analyse.
\item \code{0} — a true zero copper concentration is implausible in any environmental
matrix. A stored zero is a "not detected" entry that lost its censoring
flag somewhere upstream. Confirmed for 24 Aquatic Sediment rows from
\code{Vm_2010_2025} campaigns, which carry no LOD/LOQ and no censoring flag.
\item negative — physically impossible; usually a blank-corrected value that has
gone below zero.
}

Zero and negative values also break the log10 scales used throughout the
distribution plots, so removing them here avoids silent \code{-Inf} and \code{NaN}
propagation later.

\strong{Scope:} this filters on \code{value_col} only. It is deliberately \emph{not} a
whole-row \code{tidyr::drop_na()}, which would gut the dataset, because many
eData columns (tissue, species, uncertainty bounds, LOD/LOQ) are legitimately
sparse.

Rows dropped here are counted by \code{\link[=report_dropped_measurements]{report_dropped_measurements()}} rather than
vanishing silently.
}
