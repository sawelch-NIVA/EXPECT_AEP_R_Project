% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_outlier_groups.R
\name{get_compartment_groups}
\alias{get_compartment_groups}
\title{Get Compartment / Sub-Compartment Outlier Groups}
\usage{
get_compartment_groups(data)
}
\arguments{
\item{data}{A data frame with ENVIRON_COMPARTMENT and
ENVIRON_COMPARTMENT_SUB columns (e.g. \code{load_literature_pqt}).}
}
\value{
A tibble with one row per group: \code{.compartment},
\code{.subcompartment}, and a unique \code{.group_name} target-name fragment.
}
\description{
Identifies all distinct ENVIRON_COMPARTMENT x ENVIRON_COMPARTMENT_SUB
combinations present in \code{data}. Intended as the \code{values} argument to a
\code{tarchetypes::tar_map()} outlier-analysis factory: column names are
prefixed with \code{.} so they never collide with the real data columns
referenced (via NSE) inside the generated targets.
}
