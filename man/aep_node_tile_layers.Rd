% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_edges.R
\name{aep_node_tile_layers}
\alias{aep_node_tile_layers}
\title{A Node Drawn as a Fixed-Size Rectangle Plus Label}
\usage{
aep_node_tile_layers(data, hw, hh)
}
\arguments{
\item{data}{A nodes-like tibble carrying \code{x}, \code{y}, \code{.label}.}

\item{hw, hh}{Half-width and half-height in data units, from
\code{\link[=node_card_extent]{node_card_extent()}}.}
}
\value{
A list of ggplot2 layers.
}
\description{
Used for bare nodes (no card image) and for a node with no card among
otherwise-imaged siblings. Replaced \code{geom_label()} on 2026-08-07: a label's
background is sized to its own text, not to a shared, known extent, so an
edge had nothing geometrically real to clip to. A tile at \code{hw}/\code{hh} (from
\code{\link[=node_card_extent]{node_card_extent()}}) is the same fixed rectangle \code{\link[=aep_edge_coords]{aep_edge_coords()}}
already clips edges to when cards are drawn, so a bare diagram and the real
one agree exactly on where a node's boundary is.
}
