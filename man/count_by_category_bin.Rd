% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{count_by_category_bin}
\alias{count_by_category_bin}
\title{Count Observations per Value Bin per Category}
\usage{
count_by_category_bin(data, binwidth, origin = NULL)
}
\arguments{
\item{data}{A plot subset carrying \code{MEASURED_VALUE_STANDARD} and \code{.facet}.}

\item{binwidth}{Bin width in log10 units, from \code{\link[=category_x_binwidth]{category_x_binwidth()}}.}

\item{origin}{Left-hand end of the value axis, untransformed. \code{NULL} anchors
at the subset's own minimum.}
}
\value{
A tibble of \code{value_mid}, \code{.facet}, \code{count}. Empty bins are absent
rather than zero-filled, so they draw as panel background.
}
\description{
The counting half of the categorical heatmap. Bins are computed in log10
space, because that is the space the panel is drawn in, and the returned
\code{value_mid} is back-transformed so it can be plotted against an untransformed
\code{scale_x_log10()}.
}
\details{
Bins are anchored at \code{origin} (the left-hand end of the shared axis where one
is supplied) rather than at log10 = 0. Anchoring at zero left the outermost
bin's midpoint able to fall outside the drawn limits, so ggplot2 dropped the
tile and warned. Anchoring at the axis start also keeps bin edges identical
across every group sharing a scale, which is the point of the shared limits.
}
