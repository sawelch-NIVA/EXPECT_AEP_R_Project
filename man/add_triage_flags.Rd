% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_triage_flags.R
\name{add_triage_flags}
\alias{add_triage_flags}
\title{Add Triage Ranking Columns and Flags to the Group Summary}
\usage{
add_triage_flags(summary_data, dropped_report = NULL)
}
\arguments{
\item{summary_data}{The per-group statistics, i.e. the \code{reframe()} output in
the \code{summarise_literature_data} target.}

\item{dropped_report}{The \code{literature_dropped_report} target, or \code{NULL} to
skip the drop columns.}
}
\value{
\code{summary_data} with \code{cv}, \code{n_units}, \code{outlier_fraction}, the drop
columns, and \verb{flag_*} logicals, sorted by \code{n} descending.
}
\description{
Derives everything the triage sheet ranks or warns on, so the summary target
stays a plain set of per-group statistics and the interpretation lives here
where it can be tested.
}
\details{
\code{n_units} is the count of unit variants sharing a group's key \strong{ignoring the
unit}. Within a group the unit is constant by construction (it is part of the
key), so a per-group count would always be 1; what matters for a grouping
decision is whether the same species, tissue and place also appears in another
basis of measurement.
}
