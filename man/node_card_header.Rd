% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_node_cards.R
\name{node_card_header}
\alias{node_card_header}
\title{The Text Block at the Top of a Card}
\usage{
node_card_header(node, card, dpi = 300)
}
\arguments{
\item{node}{A one-row nodes tibble.}

\item{card}{The matching report-card row.}

\item{dpi}{The device resolution this card will be saved at. The corner id
marker is offset in PIXELS from the corner, not in points -- a physical
unit (points, inches, mm) would drift in device pixels as \code{dpi} changes,
so the offset is computed in inches from \code{dpi} right before use
(\code{offset / dpi}), matching whatever raster the card is actually saved as.
The offset itself is 2px as of 2026-08-10 (Sam cut it from 18); this
sentence used to name 18 and had gone stale, so it now points at the
constant rather than repeating it.}
}
\value{
A ggplot.
}
\description{
Label, then the four aggregation levels Sam asked for (measurements, rows,
groups, references) and the weighted centre with its unit.
}
