% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_ids.R
\name{group_code_block}
\alias{group_code_block}
\title{One Capital-Plus-Lowercase Code Block}
\usage{
group_code_block(
  parent,
  child,
  parent_codes,
  child_codes,
  parent_name,
  child_name
)
}
\arguments{
\item{parent, child}{Character vectors, same length, e.g.
\code{data$ENVIRON_COMPARTMENT} and the row-wise sub-compartment/species-group
value already selected by the caller.}

\item{parent_codes, child_codes}{Lookups from \code{\link[=read_group_code_lookup]{read_group_code_lookup()}},
keyed on \code{parent_name}/\code{child_name} respectively.}

\item{parent_name, child_name}{The lookups' key columns.}
}
\value{
A character vector of blocks, \code{NA} where either lookup misses.
}
\description{
A single axis of \code{\link[=format_composite_group_id]{format_composite_group_id()}}: a parent value's capital
letter directly followed by a child value's lowercase code, e.g. \code{"Wfw"}
(Aquatic + Freshwater) or \code{"Bl"} (Biota + Molluscs). No hyphen inside the
block -- the capital/lowercase split \emph{is} the parent/child marker (Sam
2026-08-07: "take out the hyphen between B and L... make the second letter
small so that the hierarchy is represented"), so hyphens are reserved for
separating axes from each other, not levels within one axis.
}
\keyword{internal}
