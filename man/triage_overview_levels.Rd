% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_triage_overview.R
\name{triage_overview_levels}
\alias{triage_overview_levels}
\title{Which Levels Vary Below This Parent}
\usage{
triage_overview_levels(data, stop_cols = triage_overview_stop_cols())
}
\arguments{
\item{data}{Rows under one parent node.}

\item{stop_cols}{Columns to stop after. See \code{\link[=triage_overview_stop_cols]{triage_overview_stop_cols()}}.}
}
\value{
A character vector of 0, 1 or 2 column names.
}
\description{
Returns up to two candidate columns that are both \strong{populated} and
\strong{varying} within \code{data}, stopping early at any column in \code{stop_cols}.
}
\details{
The first two conditions are why this is derived from the data rather than
fixed:
\itemize{
\item \emph{Populated.} The taxonomy columns are entirely \code{NA} for every abiotic
sub-compartment, so a fixed rule of "the next two columns" would hand
Aquatic Sediment two empty panels. Skipping them lands on site type and
site subtype, which is the comparison that sub-compartment needs.
\item \emph{Varying.} A column with one distinct value draws a single band, which
tells you nothing and costs a file and a figure slot.
}

The third is \code{stop_cols}: a level with its own node tier is \emph{included} and
then descent halts, so \verb{Biota, Aquatic} returns \code{SPECIES_GROUP} alone rather
than \code{SPECIES_GROUP} plus \code{SAMPLE_SPECIES}. Abiotic nodes are unaffected and
still return both geography levels.

Empty strings count as missing alongside \code{NA}: the decisions CSV round-trips
\code{NA} to \code{""}, so anything derived from it can carry either.
}
