% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_locator_map.R
\name{aep_locator_figure}
\alias{aep_locator_figure}
\title{A Two-Panel Geographic Locator for One AEP}
\usage{
aep_locator_figure(
  aep_id,
  manifest,
  wgs84_geo,
  sites,
  land_hires = NULL,
  poi = NULL,
  pad = 0.2
)
}
\arguments{
\item{aep_id}{Which AEP. Must have a bounding box in \code{manifest}.}

\item{manifest}{The \code{aep_manifest} table (from \code{\link[=read_aep_manifest]{read_aep_manifest()}}).}

\item{wgs84_geo}{The \code{wgs84_geography} target: a list with \code{marine_polys} and
\code{countries}. Used for panel a only.}

\item{sites}{Sampling locations. An \code{sf} of points, or a data frame with
\code{LONGITUDE} / \code{LATITUDE} columns; an \code{ENVIRON_COMPARTMENT} column, if
present, colours the points.}

\item{land_hires}{Optional pre-fetched high-resolution land \code{sf} for panel b.
\code{NULL} fetches NE 10m Norway via \code{\link[rnaturalearth:ne_countries]{rnaturalearth::ne_countries()}}.}

\item{poi}{Optional one-row data frame with \code{name}, \code{lat}, \code{lon} for a
labelled marker in panel b.}

\item{pad}{Fraction of the bounding box width/height to add as margin on
every side of panel b. \code{0.20} per Sam's spec.}
}
\value{
A \code{patchwork} of two \code{ggplot}s, \code{panel_a | panel_b}.
}
\description{
A Two-Panel Geographic Locator for One AEP
}
