% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_manifest.R
\name{write_aep_diagrams}
\alias{write_aep_diagrams}
\title{Draw and Write Every AEP}
\usage{
write_aep_diagrams(
  scoped,
  edges,
  cards,
  groups = NULL,
  card_paths = character(0),
  manifest = NULL,
  bbox_map = NULL,
  dir = here_rel("figures"),
  width = 12,
  height = 8,
  dpi = 150,
  image_size = 0.19,
  card_aspect = 1.8/2.4,
  inset_width = 0.25,
  bare = FALSE
)
}
\arguments{
\item{scoped}{Output of \code{\link[=aep_scoped_nodes]{aep_scoped_nodes()}}.}

\item{edges}{The full edges table; narrowed per AEP by \code{\link[=aep_scope_edges]{aep_scope_edges()}}.}

\item{cards}{Output of \code{\link[=aep_all_report_cards]{aep_all_report_cards()}}.}

\item{groups}{Output of \code{\link[=read_aep_node_groups]{read_aep_node_groups()}}, or \code{NULL}.}

\item{card_paths}{Compact card paths from \code{\link[=write_aep_node_cards]{write_aep_node_cards()}}.}

\item{manifest}{Output of \code{\link[=read_aep_manifest]{read_aep_manifest()}}, or \code{NULL}. Supplies the
title (\code{label}), subtitle (\code{scope_note}) and, where set, the bounding box
drawn on the locator inset. Without it, diagrams are untitled and
uninset, as before this was added.}

\item{bbox_map}{The whole-study-area map used as the locator inset's base,
e.g. the \code{wgs84_map} target. Required for an inset to be drawn; an AEP
with a bounding box but no \code{bbox_map} supplied is titled but not inset.}

\item{dir}{Where the figures go.}

\item{width, height, dpi}{Canvas, passed on to \code{\link[=node_card_extent]{node_card_extent()}} via
\code{plot_aep()} so arrow clipping matches the device actually used.}

\item{image_size}{Card width as a fraction of panel width.}

\item{card_aspect}{Compact card height over width, in inches.}

\item{inset_width}{Width of the locator inset relative to the main panel
(e.g. \code{0.25} puts it at a quarter of the main diagram's width). Ignored
when \code{bare = TRUE}, or when an AEP has no bounding box, or when
\code{bbox_map} is not supplied.}

\item{bare}{Draw text labels instead of node card images? Sam 2026-08-07:
"let's draw a bare aep without images every render/targets runthrough,
diagnosing stuff when the images are already drawn is tricky." A card
image is opaque and draws last (see \code{plot_aep()}), so it can hide an
edge or a group-box label that is actually mispositioned underneath --
exactly what made the arrowhead-clipping bug hard to pin down by eye. The
bare figure shares every geometry decision with the real one (same
coordinates, same edge clipping math, same group boxes) so a problem
visible there is a real geometry problem, not an image-occlusion
artefact, and a problem invisible there but present in the real figure IS
the occlusion. The title/subtitle/inset still apply in bare mode, since
none of those depend on the card images.}
}
\value{
The written paths, one per AEP. Named \verb{aep_<id>.png}, or
\verb{aep_<id>_bare.png} when \code{bare = TRUE}.
}
\description{
Draw and Write Every AEP
}
