% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_unit_corrections.R
\name{match_unit_correction}
\alias{match_unit_correction}
\title{Rows Matched by One Correction}
\usage{
match_unit_correction(data, correction, ids = NULL)
}
\arguments{
\item{data}{A measurements table carrying \code{row_id}.}

\item{correction}{A one-row corrections tibble.}

\item{ids}{The \code{group_ids} ledger, or \code{NULL} to skip \code{group_id} matching.}
}
\value{
A logical vector, one element per row of \code{data}.
}
\description{
Selector semantics: unset columns do not constrain, set columns combine with
AND. \code{comment_match} is a \strong{fixed} substring match, not a regex, because the
comments carry parentheses and slashes that would otherwise need escaping in
a spreadsheet cell. Both sides are passed through \code{\link[=normalise_unit_string]{normalise_unit_string()}}
first, so \code{ug/g} in the CSV matches a real micro sign in the data; write
\code{ug}, never \code{µg} (CLAUDE.md 4.4.-2).
}
