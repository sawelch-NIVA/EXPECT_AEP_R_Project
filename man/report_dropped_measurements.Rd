% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_analysis_ready.R
\name{report_dropped_measurements}
\alias{report_dropped_measurements}
\title{Report What drop_nonpositive_measurements() Removes, Per Group}
\usage{
report_dropped_measurements(
  data,
  value_col = "MEASURED_VALUE_STANDARD",
  group_cols = analysis_group_cols()
)
}
\arguments{
\item{data}{A data frame, pre-filtering (i.e. \code{load_literature_pqt}).}

\item{value_col}{Name (string) of the measured value column to filter on.}

\item{group_cols}{Character vector of grouping columns. Defaults to
\code{\link[=analysis_group_cols]{analysis_group_cols()}}.}
}
\value{
A tibble with one row per group: \code{n_input}, \code{n_na}, \code{n_zero},
\code{n_negative}, \code{n_dropped}, \code{n_retained}, \code{prop_dropped}. Sorted by
\code{n_dropped} descending, so the worst offenders are at the top.
}
\description{
Companion to \code{\link[=drop_nonpositive_measurements]{drop_nonpositive_measurements()}}. Counting the losses rather
than dropping silently is what makes the filter defensible in the methods
section, and it flags any group that is mostly non-detects before that group
is used to build an AEP node.
}
