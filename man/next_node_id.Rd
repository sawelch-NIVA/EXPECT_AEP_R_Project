% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_nodes.R
\name{next_node_id}
\alias{next_node_id}
\title{Mint the Next Composite Node Id}
\usage{
next_node_id(nodes, label)
}
\arguments{
\item{nodes}{The existing nodes table (for the highest number in use).}

\item{label}{The new node's label, to derive the slug from.}
}
\value{
A single composite node id string.
}
\description{
\verb{N<number>-<slug>}. The number is the real key (unique, never reused,
driven off the highest one already in use, same reasoning as
\code{scaffold_aep_edges.R}'s edge ids); the slug exists so \code{from}/\code{to} and
membership rows are legible without a lookup into \code{aep_nodes.csv}.
}
\details{
\strong{The slug freezes at creation.} If a node is later relabelled, its id is
NOT regenerated: doing so would require rewriting every file that names it
(\code{aep_edges.csv}, \code{aep_node_members.csv}, \code{aep_node_groups.csv},
\code{aep_membership.csv}), turning a one-cell edit into a five-file one. A
slightly stale slug is a smaller cost than that cascade.
}
