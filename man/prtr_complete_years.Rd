% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_prtr_emissions.R
\name{prtr_complete_years}
\alias{prtr_complete_years}
\title{Years Where PRTR Reporting Looks Complete}
\usage{
prtr_complete_years(
  d,
  by = c("source_category", "medium"),
  frac = 0.25,
  facility_frac = 0.5,
  min_facilities = 5
)
}
\arguments{
\item{d}{Output of \code{\link[=read_prtr_long]{read_prtr_long()}}.}

\item{by}{Grouping columns within which completeness is judged.}

\item{frac}{Fraction of the group's median total below which a year is
suspect.}

\item{facility_frac}{Fraction of the group's median reporter count below
which participation counts as collapsed.}

\item{min_facilities}{Below this typical reporter count, completeness is not
inferable and no year is flagged.}
}
\value{
A tibble of group, year, total_kg, n_facilities and \code{complete}.
}
\description{
\strong{The most recent year is routinely partial}, because facilities report on a
lag. In the 2026-08-06 extract, land-based industry falls from roughly 7,000
kg to water in 2023 to 70 kg in 2024, and from 2,000 kg to air to 10 kg. That
is a hundredfold drop in a single year across a hundred-odd facilities, which
is a submission deadline rather than an environmental event. The same thing
happens at the START of a series, where a reporting obligation is phasing in:
landfills report 0.5 kg nationally in 2009 and 0.2 kg in 2010, then 200 kg
from 2011.
}
\details{
\strong{Including those years is wrong, not merely cautious.} They enter the mean
as if they were low-emission years, so every source node magnitude comes out
understated by however many partial years the series happens to have at its
ends.

Detected rather than hard-coded, on the same principle as
\code{\link[=reach_complete_years]{reach_complete_years()}}: a year is suspect where its group total falls below
\code{frac} of that group's median year. A hard-coded cut-off year would be wrong
the moment the files are refreshed, and silently so.

\code{frac} is deliberately low. The failures this catches are one to three orders
of magnitude, so 0.25 separates them from ordinary year-to-year variation
without needing to be tuned.

\strong{THREE CONDITIONS, ALL REQUIRED}, and the first two exist because the naive
version was actively harmful on small groups. A median-of-totals rule applied
to the four Hammerfest facilities dropped 6 of 13 years: with that few
reporters, individual facility variation \emph{is} the series, so a low year is a
low year and not a missing report. The rule now:
\enumerate{
\item the group must typically have at least \code{min_facilities} reporters, or
completeness is not inferable and every year is kept;
\item this year's reporter count must have collapsed relative to the group's
median count;
\item this year's total must have collapsed relative to the group's median total.
}

Requiring both a participation collapse and a magnitude collapse is what
distinguishes a submission deadline from a genuinely clean year.
}
