% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{spatial_bin_strokes}
\alias{spatial_bin_strokes}
\title{Per-Bin Stroke Styling for the Spatial Legend}
\usage{
spatial_bin_strokes(breaks, thresholds = NULL)
}
\arguments{
\item{breaks}{The break vector from \code{\link[=spatial_colour_breaks]{spatial_colour_breaks()}}.}

\item{thresholds}{Matched thresholds from \code{\link[=thresholds_for_group]{thresholds_for_group()}}, or \code{NULL}.}
}
\value{
A list of \code{colour}, \code{linetype} and \code{linewidth} vectors, one element
per break.
}
\description{
Gives each legend bin a border in the \strong{same colour and linetype the threshold
lines use on the other panels}, so a reader who has learned "orange dashed
means entering Poor" from panel (c) reads the same thing off this key.
}
\details{
Sam's request, 2026-08-05: "re-use the vline styling but apply it to the bin
stroke." A \code{guide_colorsteps()} bar cannot do this, since it draws the bins as
one continuous strip with no per-bin styling. A \strong{binned scale rendered
through \code{guide_legend()}} can: it emits one key per bin, and \code{override.aes}
accepts a vector, so each key takes its own border.

\strong{One entry per break, not per band, and that is measured rather than
assumed.} A binned scale rendered through \code{guide_legend()} emits exactly
\code{length(breaks)} keys, each labelled with its break value: verified at 3, 5
and 15 breaks. An earlier version of this function returned
\code{length(breaks) + 1} on the reasoning that k breaks bound k + 1 bands, and
ggplot2 rejected the \code{override.aes} outright ("replacement has 16 rows, data
has 15").

Stroking the key that carries the threshold's own label is also the
unambiguous choice: it marks the boundary itself, so it is correct whichever
adjacent band that key's fill happens to show.

Keys with no threshold get \code{NA}, which draws no border at all rather than a
black one.
}
