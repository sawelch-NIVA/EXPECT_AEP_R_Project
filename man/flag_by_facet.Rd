% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_outlier_report.R
\name{flag_by_facet}
\alias{flag_by_facet}
\title{Flag Outliers Within Each Level of a Facet Column}
\usage{
flag_by_facet(data, facet_col, min_n = 10)
}
\arguments{
\item{data}{A data frame with a \code{MEASURED_VALUE_STANDARD} column.}

\item{facet_col}{Column name (string) to group by, e.g. \code{"SAMPLING_YEAR"}.}

\item{min_n}{Minimum sample size required to compute flags per level.}
}
\value{
\code{data} with flag columns added, plus a \code{facet_label} column
combining the facet level and its n (Markdown-formatted, for use as an
axis label).
}
\description{
Computes outlier flags (see \code{\link[=flag_outliers]{flag_outliers()}}) separately within each
distinct value of \code{facet_col}, e.g. so that flags reflect "is this point
unusual for its sampling year" rather than "unusual across the whole
group". Used to build the per-facet distribution plots in the generated
outlier notebooks (see \code{\link[=plot_outlier_distribution]{plot_outlier_distribution()}}).
}
