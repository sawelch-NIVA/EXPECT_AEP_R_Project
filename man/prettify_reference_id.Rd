% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{prettify_reference_id}
\alias{prettify_reference_id}
\title{Shorten an eData Reference Id for Display}
\usage{
prettify_reference_id(x)
}
\arguments{
\item{x}{A character vector of reference ids.}
}
\value{
A character vector the same length as \code{x}.
}
\description{
\code{REFERENCE_ID} is a run-on of year, first author and title
(\verb{2019SimonsenLabilityOfToxic}), up to 36 characters with no spaces in it.
As a category label that is unwrappable and unreadable at the size these
panels are drawn; as "Simonsen 2019" it is both, and the full id is one grep
away in the data.
}
\details{
The author is taken as everything before the next capitalised word, which
clips a double-barrelled surname to its first element
(\verb{2021LehmannKonEffectsOf} becomes "Lehmann 2021"). That is accepted: these
are axis labels, not citations.

Two ids shortening to the same label would silently merge two studies into
one band, so where that happens \strong{both} keep their full id rather than being
quietly conflated. Ids not matching the year-then-author shape are returned
unchanged.
}
