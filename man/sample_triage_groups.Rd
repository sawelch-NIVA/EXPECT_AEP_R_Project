% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{sample_triage_groups}
\alias{sample_triage_groups}
\title{Sample Groups for Triage}
\usage{
sample_triage_groups(
  summary_data,
  data,
  min_n = 100,
  n_sample = 5,
  seed = 20260729
)
}
\arguments{
\item{summary_data}{The \code{summarise_literature_data} target.}

\item{data}{The \code{literature_analysis_ready} target, used to count rows.}

\item{min_n}{Minimum \code{n} (measurements) for a group to be considered.}

\item{n_sample}{Number of groups to sample. \code{Inf} takes all of them.}

\item{seed}{Random seed, so the pilot selection is reproducible.}
}
\value{
A tibble of group-defining columns plus \code{n}, \code{n_sources} (distinct
REFERENCE_ID), \code{n_rows}, a filesystem-safe \code{group_slug}, a heading-anchor
\code{heading_slug} shared by every unit variant of the same group, and
\code{n_heading} (measurements summed across unit variants), sorted for
hierarchical presentation.
}
\description{
Picks groups from the summary table for which triage plots are worth making.
}
\details{
CAVEAT on \code{min_n}: \code{n} in \code{summarise_literature_data} is \code{sum(MEASURED_N)},
i.e. a count of \emph{measurements}, whereas the plots draw one mark per \emph{row}.
A group can therefore clear \code{min_n} on aggregated measurements while having
very few rows to plot. \code{n_rows} is returned alongside so you can see when
that happens.
}
