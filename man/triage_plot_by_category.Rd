% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{triage_plot_by_category}
\alias{triage_plot_by_category}
\title{Triage Plot: Distribution by a Categorical Facet}
\usage{
triage_plot_by_category(
  data,
  facet_col,
  title,
  label = NULL,
  wrap_width = 15,
  label_fn = identity,
  limits = NULL,
  thresholds = NULL,
  grp = NULL,
  x_bins = 40,
  overlay = TRUE,
  outlier_min_n = 10
)
}
\arguments{
\item{data}{A group subset.}

\item{facet_col}{Column name (string) to put on the y axis.}

\item{title}{Plot title.}

\item{label}{Group label for the subtitle.
No minimum category size is imposed. These panels answer "what campaigns and
site types are represented, and do their values differ", which is a coverage
question rather than a statistical one, so a category with two observations
is still worth seeing. Cardinality is bounded in practice (at most 31
campaigns and 5 site types per group), so this cannot produce the
unreadably tall figures that sank the first attempt.}

\item{wrap_width}{Width at which to wrap category labels.}

\item{label_fn}{Function applied to the category labels before plotting,
e.g. \code{\link[=prettify_campaign_name]{prettify_campaign_name()}}. Defaults to leaving them alone.}

\item{limits}{Shared value-axis limits from \code{\link[=triage_limits_for]{triage_limits_for()}}.}

\item{thresholds}{The \code{copper_toxicity_thresholds} target, or \code{NULL}.}

\item{grp}{The one-row group tibble, needed to match thresholds.}

\item{x_bins}{Number of bins along the value axis. The category axis is always
binned at exactly one category per bin; see below.}

\item{overlay}{Draw the boxplot, outlier ticks and count labels on top of the
heatmap. See \code{\link[=triage_category_overlay]{triage_category_overlay()}}. \code{FALSE} gives the bare heatmap.}

\item{outlier_min_n}{Minimum category size for the outlier flags to be
computed; smaller categories are reported as untested. See
\code{\link[=triage_flag_by_category]{triage_flag_by_category()}}.}
}
\value{
A ggplot.
}
\description{
Shared implementation behind the by-campaign and by-site-type plots: both
are "distribution of value, split by some category on the y axis", and
differ only in which column and how the labels are tidied.
}
