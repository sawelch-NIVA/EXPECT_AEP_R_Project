% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_manifest.R
\name{aep_scope_nodes}
\alias{aep_scope_nodes}
\title{Narrow the Node Pool to One AEP}
\usage{
aep_scope_nodes(nodes, membership, manifest, aep_id)
}
\arguments{
\item{nodes}{The nodes table from \code{\link[=read_aep_nodes]{read_aep_nodes()}}.}

\item{membership}{The table from \code{\link[=read_aep_membership]{read_aep_membership()}}.}

\item{manifest}{The table from \code{\link[=read_aep_manifest]{read_aep_manifest()}}.}

\item{aep_id}{Which AEP to scope to.}
}
\value{
A nodes tibble, restricted and repositioned.
}
\description{
Selects that AEP's nodes, takes their layout coordinates from the membership
file, and \strong{intersects} the AEP's scope with each node's own restrictions.
}
\details{
Intersection, not replacement, and the direction matters. A node restriction
is a statement about the node ("only Arctic sites of this group count as this
node"); an AEP scope is a statement about the whole diagram ("this AEP is
about Repparfjorden"). Both must hold, so the bounds narrow to the tighter of
the two. Replacing either would let an AEP quietly widen a node past the limit
its own row asserts.

The result is an ordinary nodes table, which is the point: every function
downstream of here (\code{\link[=aep_node_report_cards]{aep_node_report_cards()}}, \code{\link[=write_node_cards]{write_node_cards()}},
\code{\link[=plot_aep]{plot_aep()}}) is unchanged and does not know that AEPs exist.
}
