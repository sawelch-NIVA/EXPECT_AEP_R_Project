% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{triage_label_x}
\alias{triage_label_x}
\title{Value-Axis Anchor for the Category Count Labels}
\usage{
triage_label_x(data, limits = NULL)
}
\arguments{
\item{data}{A plot subset.}

\item{limits}{Shared value-axis limits, or \code{NULL}.}
}
\value{
A single positive value on the data scale, within \code{limits}.
}
\description{
The top of the value axis. The labels are drawn from here \strong{rightwards}
(\code{hjust = 0}) into the strip reserved by \code{\link[=triage_category_x_expansion]{triage_category_x_expansion()}}, so
they start where the data stops and never sit over a tile.
}
\details{
\strong{The anchor must stay inside the scale limits, and this is the whole reason
the function exists rather than the arithmetic being inlined.} These panels
set \code{limits} explicitly for cross-group comparability, and a continuous scale
with explicit limits censors out-of-bounds values to \code{NA} by default. A first
attempt placed the anchor 0.13 of a span \emph{beyond} \code{limits[2]}, which put it
out of bounds: every label was silently dropped and the only trace was a
\code{geom_text()} "removed N rows" warning. Expansion adds drawing room but does
not widen the limits, so the anchor is in bounds and the \emph{text} overhangs.

Falls back to the subset's own maximum where no shared limits are supplied.
}
