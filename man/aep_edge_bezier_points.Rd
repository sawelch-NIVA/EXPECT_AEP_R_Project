% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_edges.R
\name{aep_edge_bezier_points}
\alias{aep_edge_bezier_points}
\title{Quadratic Bezier Control Points for a Set of Edges}
\usage{
aep_edge_bezier_points(edges, curvature = 0.15)
}
\arguments{
\item{edges}{A clipped edges tibble with \code{x}, \code{y}, \code{xend}, \code{yend} (from
\code{\link[=aep_edge_coords]{aep_edge_coords()}}) and \code{edge_id}.}

\item{curvature}{Control-point offset as a fraction of edge length. \code{0} is
a straight line.}
}
\value{
A tibble with one row per point, \code{edge_id}, \code{.point} (1:3, start to
end) and \code{x}, \code{y}.
}
\description{
One control point per edge, offset perpendicular from the chord midpoint,
so the curve bends away from a straight line by \code{curvature} (as a fraction
of the edge's own length). Expands each edge into three rows -- start,
control point, end -- which is what \code{ggforce::geom_bezier()} needs: it
infers the curve's order from how many points share a \code{group}, so three
points draws a quadratic curve and, if this is ever extended to more
control points per edge, the same function and the same call site would
draw a genuine multi-point curve with no other change. That is the reason
this exists as ggforce rather than \code{ggplot2::geom_curve()} (2026-08-05),
which tops out at one control point.
}
\details{
Sign convention matches the old \code{geom_curve(curvature = ...)}: rotating the
edge direction 90 degrees the same way for every edge means every edge
bends to the same relative side of its own start-to-end direction, so the
diagram reads as one consistent style rather than a jumble of arbitrary
bends.
}
