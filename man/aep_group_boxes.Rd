% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_groups.R
\name{aep_group_boxes}
\alias{aep_group_boxes}
\title{Bounding Boxes for the Node Groups}
\usage{
aep_group_boxes(
  groups,
  nodes,
  pad = 0.42,
  inset = 0.13,
  card_hw = NULL,
  card_hh = NULL,
  margin = 0.15,
  label_margin = 0.06
)
}
\arguments{
\item{groups}{Output of \code{\link[=read_aep_node_groups]{read_aep_node_groups()}}.}

\item{nodes}{The nodes table, with \code{x} and \code{y}.}

\item{pad}{Flat padding at depth 0, in coordinate units. Used only when
\code{card_hw}/\code{card_hh} are \code{NULL}, i.e. there is no real card size to clear
(a text-label diagram). Shrinks with nesting depth like \code{margin} does
below.}

\item{inset}{How much \code{margin} (or, with no card context, \code{pad}) shrinks
per level of nesting.}

\item{card_hw, card_hh}{Half-width and half-height of a node card in data
units, from \code{\link[=node_card_extent]{node_card_extent()}}. When supplied, the box's mandatory
clearance is exactly this -- see \code{margin} for why nothing is ever allowed
to shrink it below the card's own edge.}

\item{margin}{Extra breathing room beyond the card's edge, in coordinate
units, at depth 0. Only this shrinks with nesting depth, floored at 0 --
the card clearance itself (\code{card_hw}/\code{card_hh}) never does.

\strong{This split is the fix for a real bug, not a style choice.} Before
2026-08-08, nesting depth was subtracted from a single combined
\code{max(pad, card_hw)} term, so a nested group (any depth >= 1) could end up
with LESS clearance than the card's own half-width -- Sam found this
directly: the "Cod" box (nested inside "Coastal") was smaller than the
node rectangles it was supposed to contain. A box must always reach
\code{min(x) - card_hw} at minimum, full stop; nesting should only tighten the
space AROUND that, which is what \code{margin} alone now does.}

\item{label_margin}{Extra clearance above the box top reserved for the
label text itself, in coordinate units, on top of whatever clears the
card.}
}
\value{
A tibble of \code{group_key}, \code{label}, \code{xmin}, \code{xmax}, \code{ymin}, \code{ymax},
\code{depth}.
}
\description{
One rectangle per group, around its members' hand-placed coordinates, inset by
depth so a nested group draws inside its parent rather than on top of it.
}
