% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{triage_minor_grid_theme}
\alias{triage_minor_grid_theme}
\title{Re-enable the Minor Grid on the Concentration Axis}
\usage{
triage_minor_grid_theme(axis = c("x", "y"))
}
\arguments{
\item{axis}{\code{"x"} where the measured value is on x (panels a, c, d), \code{"y"}
where it is on y (panel b).}
}
\value{
A ggplot2 theme.
}
\description{
Must be added \strong{after} \code{\link[=triage_theme]{triage_theme()}}, which blanks the minor grid in both
directions. Colour and linewidth are given explicitly rather than inherited:
the parent \code{panel.grid.minor} is an \code{element_blank()} at that point, and
relying on what a child inherits from a blanked parent is exactly the kind of
silent no-op that is hard to spot in a written PNG.
}
\details{
Lighter and thinner than the major grid so it reads as subordinate. Minor
lines are drawn before major ones, so a minor break coinciding with a major
break is painted over and needs no special handling.

\strong{Minor tick marks are added as well, and they are the part that matters on
the category panels.} Those panels are wall-to-wall \code{geom_tile()}, and a
geom draws over the panel grid, so inside the data region the gridlines are
invisible however dark they are: the first attempt at grey95 read as no
change at all. Ticks sit outside the panel where nothing can cover them.
}
