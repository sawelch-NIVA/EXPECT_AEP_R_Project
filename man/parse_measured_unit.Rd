% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_units.R
\name{parse_measured_unit}
\alias{parse_measured_unit}
\title{Parse a Unit into a Standard Unit and a Conversion Factor}
\usage{
parse_measured_unit(unit)
}
\arguments{
\item{unit}{A character vector of unit strings.}
}
\value{
A tibble with one row per input: \code{unit}, \code{standard} (the standard unit
or \code{NA}), \code{factor} (multiply the value by this, or \code{NA}), and \code{reason}
(why it failed to parse, or \code{NA}).
}
\description{
The whole point of this function is that the numerator and the denominator are
read \strong{separately}. See the note at the top of this file for what happens
when they are not.
}
\details{
The measurement basis (dry or wet) is required for mass-per-mass units,
because \code{mg/kg (dry)} and \code{mg/kg (wet)} are different standard units and are
not interconvertible without a moisture content this project does not hold. A
bare \code{mg/kg} is therefore \emph{not} silently assigned to either.
}
