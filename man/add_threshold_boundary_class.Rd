% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_threshold_match.R
\name{add_threshold_boundary_class}
\alias{add_threshold_boundary_class}
\title{Name Each Boundary by the Class it Opens}
\usage{
add_threshold_boundary_class(thresholds)
}
\arguments{
\item{thresholds}{A threshold table, ideally before any filtering that could
remove the open-ended top class.}
}
\value{
The same tibble with a \code{THRESHOLD_OPENS_CLASS} character column.
}
\description{
Adds \code{THRESHOLD_OPENS_CLASS}: the classification class a value enters when it
rises \strong{above} this boundary, rather than the class the boundary closes.
}
\details{
\strong{Why, 2026-08-05.} \code{THRESHOLD_VALUE} throughout this project is the \emph{upper}
boundary of the class named in \code{THRESHOLD_CLASS}. Labelling the lines that way
made every panel read one class low: the sediment line at 147 was labelled
\code{IV}, when the fact a reader wants is that everything above it is \code{V}. It also
meant Class V never appeared anywhere, because its own boundary is open-ended
and there is no line to draw for it. Sam, 2026-08-05: "it makes more sense to
use the lower bounds of the thresholds, because we're interested in knowing
when concentrations exceed it."

Under the new reading the sediment ladder labels 20 / 84 / 147 as \code{II} / \code{IV}
/ \code{V}, Class I loses its line (its lower bound is zero, which is off a log
axis anyway), and Class V gains one.

\strong{The successor is taken from the row above in the same ladder, not by adding
one to the numeral.} Copper has no Class III in M-608, so the boundary above
Class II opens Class IV; arithmetic on the numeral would invent a III that the
source does not define. Ordering is by class rank rather than by value so that
the open-ended top class, whose value is \code{NA}, still sorts last.

Rows with no class (PROREF, BAC, EQS) are left \code{NA} and fall back to their
threshold type at the label. Those are single levels rather than ladders, and
"exceeds PROREF" is already the natural reading of such a line.
}
