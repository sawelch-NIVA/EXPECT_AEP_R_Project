% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_threshold_match.R
\name{thresholds_for_group}
\alias{thresholds_for_group}
\title{Thresholds Applicable to One Sample Group}
\usage{
thresholds_for_group(
  thresholds,
  grp,
  types = c("Classification boundary", "PROREF", "BAC"),
  unit = NULL
)
}
\arguments{
\item{thresholds}{The \code{copper_toxicity_thresholds} target.}

\item{grp}{A one-row tibble of group-defining columns, from
\code{\link[=sample_triage_groups]{sample_triage_groups()}}.}

\item{types}{Threshold types to consider. The EU bioavailable EQS is excluded
by default: it is a bioavailable number and mixing it with the M-608 total /
dissolved classes on one axis invites exactly the fraction confusion this
layer is trying to avoid. It stays in the dataset, just unplotted.}

\item{unit}{The unit to match. Defaults to the group's own unit; pass it
explicitly for the unit-agnostic overall-distribution panel, which needs
one call per unit present.}
}
\value{
A tibble of matching threshold rows with \code{THRESHOLD_VALUE_STANDARD}
and a \code{threshold_label} for annotation. Zero rows where nothing applies,
which is a normal and expected outcome.
}
\description{
Open-ended classes (\code{THRESHOLD_VALUE} of \code{NA}, i.e. Very Poor) are dropped:
there is no line to draw for "everything above the last boundary".
}
