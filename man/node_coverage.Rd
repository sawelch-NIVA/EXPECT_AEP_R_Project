% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_nodes.R
\name{node_coverage}
\alias{node_coverage}
\title{What Has Not Been Claimed by Any Node}
\usage{
node_coverage(members, summary_data, ids, decisions = NULL)
}
\arguments{
\item{members}{The membership table.}

\item{summary_data}{The \code{summarise_literature_data} target.}

\item{ids}{The group id ledger.}

\item{decisions}{Optional decisions table, to carry \code{decision} through so a
group deliberately dropped is distinguishable from one never looked at.}
}
\value{
A tibble, one row per group, with \code{node_id} (or \code{NA}) and \code{claimed}.
}
\description{
\strong{The backlog view, and the reason it exists.} Sam abandoned sequential review
of all 245 groups on 2026-08-05 in favour of picking groups of interest and
expanding outwards. That is the right call, but it needs the complement: a
ranked list of what has \emph{not} been picked, so stopping is an informed choice
rather than an omission nobody noticed.
}
\details{
Ranked by measurements descending, so the largest unclaimed group is always
the first thing on screen.
}
