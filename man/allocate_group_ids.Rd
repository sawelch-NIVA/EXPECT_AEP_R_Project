% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_ids.R
\name{allocate_group_ids}
\alias{allocate_group_ids}
\title{Allocate IDs to Any Groups That Lack One}
\usage{
allocate_group_ids(
  summary_data,
  path = here::here("data/clean/decisions/group_ids.csv"),
  verbose = TRUE
)
}
\arguments{
\item{summary_data}{The \code{summarise_literature_data} target.}

\item{path}{Path to the ledger CSV.}

\item{verbose}{Report what changed?}
}
\value{
The full ledger, invisibly.
}
\description{
Append-only. Existing IDs are never changed and \strong{retired IDs are never
reused}: the next ID is always one past the highest ever issued, not one past
the highest currently in use. Reuse is how a note written in March ends up
pointing at a different group in September.
}
\details{
Allocation order for a fresh ledger is by \code{n} descending, which is arbitrary
but harmless because it is frozen immediately.
}
