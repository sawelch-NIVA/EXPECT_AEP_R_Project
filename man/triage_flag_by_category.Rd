% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{triage_flag_by_category}
\alias{triage_flag_by_category}
\title{Per-Category Outlier Flags for a Triage Panel}
\usage{
triage_flag_by_category(data, min_n = 10)
}
\arguments{
\item{data}{A plot subset carrying \code{MEASURED_VALUE_STANDARD} and \code{.facet}.}

\item{min_n}{Minimum category size for the flags to be computed.}
}
\value{
\code{data} with \code{.outlier} (logical, \code{FALSE} where untested) and
\code{.tested} (logical) added.
}
\description{
Recomputes the two outlier criteria \strong{within each category of the panel}
(each campaign or reference on panels c and d), rather than reusing the
sample-group flags that \code{summarise_literature_data} puts in the triage table.
}
\details{
Why the difference is deliberate, and why both are right (Sam, 2026-08-04:
"we're doing the same operation here but at a different level of grouping"):
\itemize{
\item The sample-group fence pools every campaign in the group. Measured on
Aquatic / Freshwater / River, that made \strong{one} mining campaign produce 998
of the group's 1,316 flags, purely by being a mining campaign. Its values
are unremarkable for a mine and extreme for the group.
\item The per-campaign fence asks whether a value is unusual \emph{for its own
campaign}. On the same group it flags 687 rows, Mine Impact drops to 0, and
dispersed campaigns that the pooled fence hid (Reference Rivers, 2 to 53)
become visible.
}

Neither subsumes the other, and this does \strong{not} change
\code{n_double_outliers} or anything the triage table ranks on.

Categories below \code{min_n} are not tested at all, and are reported as
\code{tested = FALSE} rather than as zero. A fence fitted to a handful of points
is noise: Urban Fjord Contaminants has 7 rows in this group, two of which are
the 1e3 block, and no fence fitted to that sample can call 29\% of itself an
outlier. Such rows say so in their label; see \code{\link[=triage_category_labels]{triage_category_labels()}}.
}
