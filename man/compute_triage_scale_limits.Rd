% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{compute_triage_scale_limits}
\alias{compute_triage_scale_limits}
\title{Compute Shared Value-Axis Limits}
\usage{
compute_triage_scale_limits(data, by = "ENVIRON_COMPARTMENT", pad = 1.5)
}
\arguments{
\item{data}{The \code{literature_analysis_ready} target.}

\item{by}{Columns defining a scale group.
The \strong{date} range is deliberately global and never grouped. Time is the one
axis where a per-group scale is always wrong: a group sampled only in 2019
would otherwise fill the whole panel and look identical to one sampled over
thirty years.}

\item{pad}{Multiplicative padding applied to each end of the value axis, so
points do not sit exactly on the panel edge. Dates are not padded.}
}
\value{
A tibble of \code{by} columns plus \code{value_min}, \code{value_max}, and the
global \code{date_min} / \code{date_max}.
}
\description{
Triage plots are only comparable if they share axes. Computing limits from
each group's own data (the ggplot2 default) means every panel silently
rescales, so two groups an order of magnitude apart can look identical.
This derives limits once from the whole dataset and they are then passed
into every plot.
}
\details{
Grouped by \code{ENVIRON_COMPARTMENT} by default. Be aware of what that does and
does not buy you: as of 2026-07-29 Aquatic alone spans 12.3 orders of
magnitude, which is the entire global range, so for ~90\% of the rows a
per-compartment limit is a global limit. Adding \code{MEASURED_UNIT_STANDARD} to
\code{by} only narrows Aquatic/mg-L to 9.8 orders; the spread is genuinely within
unit, not an artefact of mixing them. Widen or narrow via \code{by} as needed.

No epsilon is added for the log scale: \code{literature_analysis_ready} has
already dropped zeros and negatives, so every value is strictly positive.
}
