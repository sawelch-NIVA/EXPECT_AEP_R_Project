% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_edge_cards.R
\name{write_aep_edge_cards}
\alias{write_aep_edge_cards}
\title{Write an Edge Card per Non-Rejected Edge, per AEP}
\usage{
write_aep_edge_cards(
  scoped,
  edges,
  dir = here_rel("figures/edge_cards"),
  width = 2.4,
  height = 1.6,
  dpi = 300
)
}
\arguments{
\item{scoped}{Output of \code{\link[=aep_scoped_nodes]{aep_scoped_nodes()}}.}

\item{edges}{The full edges table.}

\item{dir}{Parent directory; each AEP gets a subdirectory of it.}

\item{width, height, dpi}{Canvas. Smaller than a node card (2.4 x 1.8): the
edge card has two bands, not three, and no distribution to make room for.}
}
\value{
The written paths, across all AEPs.
}
\description{
One PNG per live edge into \verb{dir/<aep_id>/<edge_id>.png}, mirroring
\code{\link[=write_aep_node_cards]{write_aep_node_cards()}}. An edge belongs to an AEP when both its endpoints
do (\code{\link[=aep_scope_edges]{aep_scope_edges()}}); rejected edges are dropped (\code{\link[=drop_rejected_edges]{drop_rejected_edges()}}).
}
\details{
The card content does not vary by AEP -- magnitude and scores are hand-entered
on the edge, not recomputed under a scope -- so the same edge produces an
identical card in each AEP it appears in. The per-AEP subdirectories exist so
the layout matches the node cards for hand assembly.
}
