% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_literature_heatmap.R
\name{plot_reference_compartment_heatmap}
\alias{plot_reference_compartment_heatmap}
\title{Plot Reference-Compartment Heatmap}
\usage{
plot_reference_compartment_heatmap(
  data,
  compartment_order = NULL,
  x_var = "ENVIRON_COMPARTMENT_SUB",
  y_var = "REFERENCE_ID",
  year_var = "YEAR",
  fill_label = "Sample Size (n)",
  x_label = "Environmental Sub-Compartment",
  text_threshold = "mean",
  rotate_x = 45
)
}
\arguments{
\item{data}{A data frame containing the main table data}

\item{compartment_order}{Character vector of compartment names in desired order.
If NULL, will attempt to use \code{environ_compartments_sub_vocabulary()}.}

\item{x_var}{Character string specifying the column name for x-axis (compartment).
Default is "ENVIRON_COMPARTMENT_SUB".}

\item{y_var}{Character string specifying the column name for y-axis (reference).
Default is "REFERENCE_ID".}

\item{year_var}{Character string specifying the column name for year ordering.
Default is "YEAR".}

\item{fill_label}{Character string for the legend title. Default is "Sample Size (n)".}

\item{x_label}{Character string for x-axis label. Default is "Environmental Sub-Compartment".}

\item{text_threshold}{Threshold for switching text color from black to white.
Can be "mean", "median", or a numeric value. Default is "mean".}

\item{rotate_x}{Angle to rotate x-axis labels. Default is 45.}
}
\value{
A ggplot2 object
}
\description{
Creates a heatmap showing the count of observations for each combination of
reference and environmental compartment, with references ordered by year.
}
\examples{
\dontrun{
plot_reference_compartment_heatmap(main_table)
plot_reference_compartment_heatmap(main_table, text_threshold = "median")
plot_reference_compartment_heatmap(main_table, text_threshold = 5)
}
}
