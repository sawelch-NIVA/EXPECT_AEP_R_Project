% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{triage_category_overlay}
\alias{triage_category_overlay}
\title{Distribution Overlay for the Categorical Panels}
\usage{
triage_category_overlay(data, limits = NULL, min_n = 10, ticks = TRUE)
}
\arguments{
\item{data}{Output of \code{\link[=triage_flag_by_category]{triage_flag_by_category()}}.}

\item{limits}{Shared value-axis limits, used to place the right-margin
labels. Falls back to the data's own maximum where absent.}

\item{min_n}{Passed to \code{\link[=triage_category_labels]{triage_category_labels()}} for the untested wording.}

\item{ticks}{Draw the outlier ticks. \code{FALSE} on the small-n panels, where
every observation is already drawn as a point and ticks would be clutter.}
}
\value{
A list of ggplot2 layers.
}
\description{
The layers drawn on top of the heatmap: a boxplot per category, tick marks at
the per-category outliers, and the count labels in the right margin.
}
\details{
\strong{These layers must be added before \code{\link[=triage_threshold_layers]{triage_threshold_layers()}}.} The
threshold lines have to draw last or the boxplot covers them, which was the
first thing wrong with this overlay.

Design notes, all settled by rendering against the real data on 2026-08-04:
\itemize{
\item \strong{\code{outlier.shape = NA} is mandatory.} The boxplot's own outlier points are
one mark per row, which on a 42,000-row group is precisely the thing
CLAUDE.md 4.4 forbids. Suppressed here; the tick layer draws the outliers
that survive the stricter two-criteria test instead.
\item \strong{The box is drawn twice}, a dark halo under a white line. The viridis
fill is light at the modal bins, which is exactly where the box sits, so a
dark line alone is swallowed there; the reverse pairing (white halo, dark
line) was tried and put fat white whiskers through the tile rows.
\item \strong{Ticks, not points.} Points overplot into solid runs on the dense
categories; ticks read as texture, and the density of the fringe is itself
informative. White, not red: red over-dramatised what is mostly ordinary
distribution tail, and white borrows the language the box already set.
\item \strong{\code{width = 0.45}} leaves a visible sliver of the tile band above and below,
so the row still reads as a heatmap row rather than as a broken boxplot.
}

Box statistics are computed in log10 space, because \code{\link[=triage_value_scale]{triage_value_scale()}}
transforms before the stat runs. Quartiles are unaffected (quantiles are
equivariant under a monotone transform) but the whiskers are 1.5 x IQR \strong{in
log units}, which is the same fence \code{\link[=flag_outliers]{flag_outliers()}} uses and is the right
definition for data spanning this many decades.
}
