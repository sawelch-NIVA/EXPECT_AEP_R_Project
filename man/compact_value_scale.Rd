% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_node_cards.R
\name{compact_value_scale}
\alias{compact_value_scale}
\title{Value Axis for a Compact Strip}
\usage{
compact_value_scale(limits = NULL, every = 3, thresholds = NULL)
}
\arguments{
\item{limits}{Shared value limits for the node's unit.}

\item{every}{Decades between labelled breaks.}

\item{thresholds}{Output of \code{\link[=thresholds_for_group]{thresholds_for_group()}}, or \code{NULL}. Where
given, adds a secondary axis on top naming each threshold's class in
roman numerals (I, II, III, ...) at the value it sits on. Sam asked for
the class levels on the compact card but nothing else the full secondary
axis carries, so unlike \code{\link[=triage_threshold_sec_axis]{triage_threshold_sec_axis()}} this omits the axis
title (source and matrix): at card width there is no room for it and it
would compete with the class numerals it is meant to explain.}
}
\value{
A ggplot2 scale.
}
\description{
A tick at every decade, a labelled major break every third decade. Sam
2026-08-06: "violin plots need \emph{some} kind of x axis or they're fairly
meaningless", and at card width a label per decade is thirteen numbers in two
inches.
}
\details{
\strong{Order of operations is load-bearing, and Sam flagged it before I hit it.}
A complete theme (\code{theme_void()}, \code{theme_minimal()}) replaces the whole theme
object rather than merging into it, so any axis styling added BEFORE one is
silently discarded. Everything here is applied after the complete theme, and
\code{\link[=compact_axis_theme]{compact_axis_theme()}} is written to be added last.
}
