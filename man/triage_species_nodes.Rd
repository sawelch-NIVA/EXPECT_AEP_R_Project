% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_triage_species.R
\name{triage_species_nodes}
\alias{triage_species_nodes}
\title{Species Groups Worth a By-Species Panel}
\usage{
triage_species_nodes(data, min_n = 100, groups = NULL)
}
\arguments{
\item{data}{The \code{literature_analysis_ready} target. Must carry \code{MEASURED_N}.}

\item{min_n}{Minimum measurements for a node to qualify.}

\item{groups}{The \code{triage_pilot_groups} target. Species groups with no
displayed group beneath them are dropped, since the notebook never opens a
heading for them; see \code{\link[=filter_reachable_nodes]{filter_reachable_nodes()}}. \code{NULL} skips the check.}
}
\value{
A tibble of node columns plus \code{n}, \code{n_rows}, \code{n_bands}, \code{n_species},
\code{n_tissues}, \code{node_label} and \code{node_slug}.
}
\description{
One row per compartment x sub-compartment x species group x unit carrying at
least \code{min_n} measurements across at least two distinct species/tissue bands.
}
\details{
The two-band minimum is the same "must vary" rule the sub-compartment tier
uses: a by-species panel showing one band answers nothing. It is what drops
\verb{Biota, Terrestrial / Mammals / mg/kg (dry)}, which reaches 317 measurements
but is \emph{Ursus maritimus} liver and nothing else.
}
