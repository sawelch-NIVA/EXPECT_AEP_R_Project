% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_edges.R
\name{plot_aep}
\alias{plot_aep}
\title{Draw the AEP}
\usage{
plot_aep(
  nodes,
  edges,
  cards = NULL,
  label_edges = TRUE,
  groups = NULL,
  node_images = NULL,
  image_size = 0.16,
  card_aspect = 1.8/2.4,
  device_aspect = 12/8,
  curvature = 0.15,
  tile_size = NULL,
  tile_aspect = NULL,
  x_expand = 0.15,
  y_expand = 0.12
)
}
\arguments{
\item{nodes}{The nodes table, with \code{x} and \code{y} populated.}

\item{edges}{The edges table.}

\item{cards}{Optional report cards from \code{\link[=aep_node_report_cards]{aep_node_report_cards()}}. When
supplied, each node label gains its geometric mean and unit.}

\item{label_edges}{Annotate empirical edges with their magnitude?}

\item{groups}{Optional node groups from \code{\link[=read_aep_node_groups]{read_aep_node_groups()}}, drawn as
labelled boxes behind everything else. Nested groups inset automatically;
see \code{\link[=aep_group_depth]{aep_group_depth()}}.}

\item{node_images}{Optional named character vector of PNG paths keyed by
\code{node_id}, from \code{\link[=write_node_cards]{write_node_cards()}}. Where
supplied, each node is drawn as its card instead of a text label. This is
PLAN.md P5.2, and it is what makes the figure a report-card AEP rather than
a labelled graph.}

\item{image_size}{Card width as a fraction of plot width.}

\item{card_aspect, device_aspect}{Card and device shape, passed to
\code{\link[=node_card_extent]{node_card_extent()}} so arrows can be clipped to the cards. Defaults match
the \code{aep_diagram} target; pass them explicitly from anywhere else.}

\item{curvature}{Bend applied to every edge, passed to
\code{\link[=aep_edge_bezier_points]{aep_edge_bezier_points()}}. \code{0} is a straight line.}

\item{tile_size, tile_aspect}{Bare-node rectangle width (as a fraction of
panel width) and height-over-width, used only when \code{node_images} is not
supplied. \code{NULL} (the default for both) means "the same size as the real
card": falls back to \code{image_size}/\code{card_aspect}, so a bare diagram and the
real one draw identically sized boxes without having to keep two sets of
numbers in sync by hand. Sam 2026-08-07: the first cut of this used its
own guessed defaults (0.14, 0.45) instead, and they came out roughly half
the real card's size (the real compact card is 2.4in x 1.8in at 200dpi,
i.e. 480x360px, \code{card_aspect = 1.8/2.4 = 0.75}, not the 0.45 guessed
here) -- pass them explicitly only if a bare diagram should deliberately
look different from the real cards.}

\item{x_expand, y_expand}{Axis margin as a fraction of the node coordinate
span, applied before any group box is taken into account. The single
source of truth for this: previously \code{node_card_extent()}'s defaults and
this function's own axis \verb{expand=} were two separately hardcoded 0.15/
0.12 pairs that had to be kept in sync by hand and were the seed of the
2026-08-07/08 tile-sizing bug (see \code{\link[=node_card_extent]{node_card_extent()}}'s \code{x_range}/
\code{y_range} doc). Now there is one value, threaded through everywhere it
matters.}
}
\value{
A ggplot.
}
\description{
\strong{Manual coordinates, never an automatic layout.} Vertical position carries
source-to-exposure meaning, so a force-directed or hierarchical layout is not
merely different, it is wrong: it would place nodes to minimise edge crossings
and in doing so destroy the one thing the y axis is for. PLAN.md P5.1.
}
\details{
Plain ggplot2 rather than ggraph, on the "boring fix that ships" principle in
CLAUDE.md. With coordinates already fixed there is no layout algorithm left to
want, and a graph package would add a dependency to draw segments and text.
}
