% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_manifest.R
\name{aep_diagram_height}
\alias{aep_diagram_height}
\title{Canvas Height That Scales With Node Density}
\usage{
aep_diagram_height(
  nodes,
  effective_width,
  image_size,
  card_aspect,
  x_expand = 0.15,
  y_expand = 0.12,
  min_height = 8,
  fill_fraction = 0.6
)
}
\arguments{
\item{nodes}{The AEP's own scoped, placed nodes tibble.}

\item{effective_width}{Inches, from \code{\link[=aep_diagram_squeeze]{aep_diagram_squeeze()}}'s
\code{effective_width} (i.e. already accounting for a locator inset, if any).}

\item{image_size, card_aspect}{As elsewhere; use the SQUEEZE-COMPENSATED
\code{image_size} (\code{aep_diagram_squeeze()}'s own output), not the raw one.}

\item{x_expand, y_expand}{As \code{\link[=plot_aep]{plot_aep()}}; keep these in sync with whatever
is actually passed to \code{plot_aep()} for the real render, since this
function has to predict the same \code{hh} \code{node_card_extent()} will.}

\item{min_height}{Floor, inches. Never returns less than this.}

\item{fill_fraction}{How much of the tightest row's vertical space a card
may occupy at most, leaving \code{1 - fill_fraction} as a visible gap between
adjacent rows. \code{0.6} errs toward "clearly separated" over "tightly
packed": the group boxes drawn behind everything add their own visual
weight, so rows sitting right on top of each other read as more crowded
than the same numeric gap does on a plain grid.}
}
\value{
Height in inches, \verb{>= min_height}.
}
\description{
A node card's PHYSICAL size (in inches) is fixed by \code{image_size} and the
panel's WIDTH alone -- see the note at the top of \code{\link[=node_card_extent]{node_card_extent()}}'s
\code{hh} calculation: \code{ggimage::geom_image()} preserves the image's own aspect
ratio regardless of device shape, so widening or narrowing the canvas
HEIGHT never changes how big a card actually is. What it changes is how
many data-y-units fit in that fixed physical height: a taller canvas packs
more inches into the same y-range, i.e. more room between rows. That is the
whole mechanism this function uses.
}
\details{
Sam 2026-08-08, once the reindexed node set gave "10 or so organism nodes"
stacked in one column at a fixed 12x8in canvas: "we can't especially afford
to put them in a 1x10 column ... it may be time to start thinking about
ggraph". Ruled out (see chat): vertical position is semantically meaningful
here (CLAUDE.md, \code{plot_aep()}'s own header), so an automatic layout is off
the table regardless of library. The actual two causes of the overlap were
cheaper: canvas size was a constant regardless of node count, and this
function fixes exactly that half, automatically, from the AEP's own layout.
(Widening the canvas would NOT help the companion problem of several nodes
sharing a column at close x-spacing -- \code{hw} has no such width dependency to
exploit, since \code{image_size} is already a fraction of panel width by
definition. That half stays a manual layout decision.)
}
