% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_threshold_match.R
\name{threshold_class_number}
\alias{threshold_class_number}
\title{Classification Class Number of a Threshold}
\usage{
threshold_class_number(cls)
}
\arguments{
\item{cls}{A character vector of \code{THRESHOLD_CLASS} values.}
}
\value{
A factor with levels \code{I} to \code{V}.
}
\description{
Extracts the roman numeral from a \code{THRESHOLD_CLASS} such as \code{Background (I)}
or the coastal \code{Good - Moderate (II-III)} (which yields \code{II}). PROREF and BAC
carry no class; both are background values, so they take \code{I}.
}
\details{
The alternation below is ordered \strong{longest first, and that is load-bearing.}
Regex alternation is ordered, so \verb{(I\{1,3\}|IV|V)} matches the bare \code{I} inside
\code{(IV)} and never tries \code{IV}. That silently styled Poor as Background: blue and
dotted rather than orange and near-solid. Caught by eye on a prototype, not by
any numeric check.

Keying style on the class \emph{number} rather than on position within the classes
present matters because copper has no Class III (see
\code{generate_copper_thresholds()}). Poor must still read as the fourth class, and
a dataset that does have a Class III must pick up the third style without
anything being reordered.
}
