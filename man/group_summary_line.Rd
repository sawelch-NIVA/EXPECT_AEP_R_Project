% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_triage_flags.R
\name{group_summary_line}
\alias{group_summary_line}
\title{One-Line Summary Sentence for a Triage Group}
\usage{
group_summary_line(row)
}
\arguments{
\item{row}{A one-row data frame carrying the columns from
\code{\link[=add_triage_flags]{add_triage_flags()}} plus \code{n_rows} and, optionally, \code{reference_ids} (a
comma-separated string from \code{\link[=sample_triage_groups]{sample_triage_groups()}}).}
}
\value{
A single markdown string.
}
\description{
Everything that goes under a group heading in the triage notebook: the counts,
the unit, and any flags. Built here rather than in the notebook so the wording
is testable and cannot drift from the table's highlighting.
}
\details{
Reports counts, not statistics. Any summary statistic added here has to be one
Sam can defend in the methods section, which is why the CV that briefly lived
here is gone.
}
