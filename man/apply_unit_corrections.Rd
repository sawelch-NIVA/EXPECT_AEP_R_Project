% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_unit_corrections.R
\name{apply_unit_corrections}
\alias{apply_unit_corrections}
\title{Apply Unit Corrections to the Measurements Table}
\usage{
apply_unit_corrections(data, corrections, ids = NULL, id_col = "row_id")
}
\arguments{
\item{data}{A measurements table carrying \code{row_id}.}

\item{corrections}{A validated corrections tibble.}

\item{ids}{The \code{group_ids} ledger.}

\item{id_col}{Name of the row id column.}
}
\value{
\code{data} with values scaled and two provenance columns added.
}
\description{
Scales \code{\link[=unit_correction_value_cols]{unit_correction_value_cols()}} by each correction's \code{factor}, and
records provenance in \code{unit_correction_id} and \code{unit_correction_factor}.
}
\details{
Aborts, never warns, on:
\itemize{
\item a selector matching no rows (the correction is stale, and a stale
correction that silently does nothing is the failure mode this whole layer
was built to avoid);
\item a selector whose matched rows differ from the recorded \code{row_ids}, in either
direction, naming the drift both ways;
\item a row matched by two corrections, since the order of compounding would then
decide the answer.
}
}
