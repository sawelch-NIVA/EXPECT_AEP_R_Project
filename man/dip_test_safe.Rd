% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_outlier_detection.R
\name{dip_test_safe}
\alias{dip_test_safe}
\title{Hartigan's Dip Test for Unimodality, Gated by Minimum Sample Size}
\usage{
dip_test_safe(x, min_n = 10)
}
\arguments{
\item{x}{A numeric vector of measured values.}

\item{min_n}{Minimum sample size required to run the test.}
}
\value{
A list with \code{dip_p} (p-value, or \code{NA}) and \code{multimodal} (logical:
\code{TRUE} where unimodality is rejected at \code{dip_p < 0.05}, or \code{NA} where the
test was not run).
}
\description{
Tests the null hypothesis that \code{x} is drawn from a unimodal distribution.
A significant result means unimodality is \emph{rejected}; it does not identify
how many modes there are, and in particular it is not a test for
bimodality specifically. Hence \code{multimodal} rather than \code{bimodal}.
}
\details{
Below \code{min_n}, the test is not run (it is unreliable, and errors outright
below n = 4). TODO: Add citation
}
