% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{triage_category_header}
\alias{triage_category_header}
\title{Header for the Right-Margin Count Column}
\usage{
triage_category_header(data, x_at, size = 2.1)
}
\arguments{
\item{data}{Output of \code{\link[=triage_flag_by_category]{triage_flag_by_category()}}.}

\item{x_at}{Value-axis position, shared with the labels themselves.}
}
\value{
A ggplot2 layer, or \code{NULL} where there is nothing to head.
}
\description{
\code{"n (n outliers)"}, sitting above the topmost category label. Sam's call,
2026-08-05: the bracketed second figure was unexplained, and a reader had no
way to tell it from a second sample size.
}
\details{
Placed at a fractional position above the last discrete level. Discrete scales
carry a continuous range alongside the discrete one, which is what lets the
outlier ticks sit at \code{as.numeric(.facet) - 0.46}; the same mechanism puts this
half a band above the top row. \code{\link[=triage_plot_by_category]{triage_plot_by_category()}} widens the upper
expansion to make the room.
}
