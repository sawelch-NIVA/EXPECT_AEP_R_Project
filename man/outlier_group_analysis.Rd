% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_outlier_detection.R
\name{outlier_group_analysis}
\alias{outlier_group_analysis}
\title{Run the Full Outlier Analysis for One Group}
\usage{
outlier_group_analysis(data, group_label, min_n = 10)
}
\arguments{
\item{data}{A pre-filtered data frame for a single group (compartment,
species/tissue, etc.), with a \code{MEASURED_VALUE_STANDARD} column.}

\item{group_label}{A human-readable label for the group, used in plot
titles etc.}

\item{min_n}{Minimum sample size required to compute outlier flags, dip
test, and Winsorization bounds; below this, a distribution can still be
plotted, but the statistics are not trustworthy enough to compute.}
}
\value{
A list: \code{group_label}, \code{data} (row-level data with flag and
Winsorized-value columns added), \code{n}, \code{n_dropped_unit} (rows excluded
for using a non-dominant unit), \code{min_n}, \code{stats_computed} (logical),
\code{dip_p}, \code{multimodal}, and \code{summary} (one-row tibble of descriptive stats).
}
\description{
Restricts \code{data} to its dominant measured unit, then computes outlier
flags, a dip test, and Winsorized value columns (all gated by \code{min_n}),
plus descriptive summary statistics (always computed, regardless of n).
}
\details{
Intended to be called once per branch of a \code{tarchetypes::tar_map()}
factory, with \code{data} already pre-filtered to the group of interest (see
\code{\link[=prepare_compartment_group_data]{prepare_compartment_group_data()}} / \code{\link[=prepare_biota_group_data]{prepare_biota_group_data()}}).
}
