% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_edges.R
\name{aep_bbox_inset}
\alias{aep_bbox_inset}
\title{A Small Bounding-Box Locator Map}
\usage{
aep_bbox_inset(base_map, lat_min, lat_max, lon_min, lon_max)
}
\arguments{
\item{base_map}{The whole-study-area map, e.g. the \code{wgs84_map} target
(\code{\link[=create_study_area_map_wgs84]{create_study_area_map_wgs84()}}).}

\item{lat_min, lat_max, lon_min, lon_max}{Bounding box; \code{NA} on any side means
unbounded and is drawn against the full plotted extent of \code{base_map} on
that side.}
}
\value{
A ggplot, stripped down for use as a side panel.
}
\description{
For a geographically scoped AEP (a manifest row with any of \code{lat_min},
\code{lat_max}, \code{lon_min}, \code{lon_max} set), shows where its bounding box sits
against the whole study area, so the diagram does not have to be read next
to the manifest to know what "Repparfjorden" is scoped to.
}
