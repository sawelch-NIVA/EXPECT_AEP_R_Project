% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{triage_theme}
\alias{triage_theme}
\title{Shared Theme for the Triage Panels}
\usage{
triage_theme()
}
\value{
A list of ggplot2 theme components.
}
\description{
\code{theme_minimal()} plus the tweaks every panel wants. One function so the look
can be changed in one place.
}
\details{
\strong{This has to be added before any per-panel \code{theme()} call.}
\code{theme_minimal()} replaces the whole theme, whereas \code{theme()} modifies it, so
\code{theme(legend.position = "bottom") + theme_minimal()} silently discards the
legend position. Every call site below therefore puts this first.

It also cannot live inside \code{\link[=triage_threshold_layers]{triage_threshold_layers()}}: that returns a bare
list of layers, and ggplot2 refuses to add a theme to a geom outside a plot
("Cannot add ggproto objects together").
}
