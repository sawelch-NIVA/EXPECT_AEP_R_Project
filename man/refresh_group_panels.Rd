% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_notebooks.R
\name{refresh_group_panels}
\alias{refresh_group_panels}
\title{Give Panels to Sections That Have Since Gained Them}
\usage{
refresh_group_panels(
  groups,
  dir = here_rel("docs/groups"),
  decisions = NULL,
  verbose = TRUE
)
}
\arguments{
\item{groups}{The \code{triage_pilot_groups} target, supplying \code{group_id} and
\code{group_slug}.}

\item{dir}{Where the notebooks live.}

\item{decisions}{Optional decisions table, used only for the group label in
the figure caption. Falls back to the label already in the heading.}

\item{verbose}{Report what changed?}
}
\value{
A tibble of \code{file}, \code{group_id}, \code{repaired}, invisibly.
}
\description{
\strong{The gap this closes.} Adding a group to \code{must_include} and re-running the
pipeline writes its five PNGs, but the notebook section still reads "No triage
panels: this group is below the \code{min_n} cutoff". The pipeline never touches
\verb{docs/groups/*.qmd} at all, and \code{\link[=generate_group_notebooks]{generate_group_notebooks()}} cannot help
either: it appends sections only for group anchors ABSENT from the file, so a
section that already exists is skipped whatever has changed underneath it.
}
\details{
Sam hit exactly this with G047 on 2026-08-05, having correctly edited the
target and re-run the pipeline twice.

\strong{Append-only is not weakened, and the scoping is the whole argument.} This
replaces one line, and only where that line is byte-identical to
\code{\link[=no_panels_placeholder]{no_panels_placeholder()}} for the group whose section it sits in. That string
is machine-written boilerplate that no one would type. If Sam has written a
single character into it, the match fails and the section is left alone. It
cannot touch prose, a verdict, or a section that already has a figure block.

Idempotent: after a repair the placeholder is gone, so re-running does
nothing.
}
