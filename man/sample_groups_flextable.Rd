% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_sample_groups_table.R
\name{sample_groups_flextable}
\alias{sample_groups_flextable}
\title{Render the Sample-Groups Table as a flextable}
\usage{
sample_groups_flextable(tbl, link_sections = NULL)
}
\arguments{
\item{tbl}{Output of \code{\link[=build_sample_groups_table]{build_sample_groups_table()}}, optionally filtered.}

\item{link_sections}{Character vector of anchors present in this document, or
\code{NULL} to link nothing.}
}
\value{
A \code{flextable} object.
}
\description{
Applies display labels and conditional formatting. Row indices for the
highlighting are computed from whatever \code{tbl} is passed in, so filtering the
table before calling this (as the manuscript does, to show only large
groups) keeps the highlighting aligned.
}
\details{
Linking is opt-in. Pass \code{link_sections} the heading anchors that actually
exist as sections in the calling document (from
\code{sample_triage_groups()$heading_slug}) and those rows get their \code{group} cell
rendered as an internal link; every other row stays plain text, so the
220-odd untriaged groups do not advertise dead anchors. The default of \code{NULL}
links nothing, which is what \code{index.qmd} needs: it reads the same target but
contains none of the sections, and its docx output would carry the dead links
outward.
}
