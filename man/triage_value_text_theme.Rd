% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{triage_value_text_theme}
\alias{triage_value_text_theme}
\title{De-emphasise the Concentration-Axis Labels}
\usage{
triage_value_text_theme(axis = c("x", "y"))
}
\arguments{
\item{axis}{\code{"x"} where the measured value is on x (panels a, c, d), \code{"y"}
where it is on y (panel b).}
}
\value{
A ggplot2 theme.
}
\description{
The shared per-compartment limits put up to 13 decade labels on the value
axis (see \code{\link[=triage_value_scale]{triage_value_scale()}}), and at full weight that row of \code{1e-07},
\code{1e-06}, ... competes with the panel for attention when it is really just a
ruler. Smaller and greyer, so it reads as scale furniture.
}
\details{
Font size rather than shortening the labels. Dropping the mantissa to leave
\code{-07}, \code{-06} was the other option Sam offered and is fewer characters, but a
bare exponent is not a concentration and the axis title does not say
"log10", so it invites a reader to take -07 as a value. The labels stay as
\code{\link[=triage_value_scale]{triage_value_scale()}} writes them.

Applied to one axis only. The category axis on panels c and d already sets
its own size, and the two must not fight.
}
