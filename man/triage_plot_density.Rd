% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{triage_plot_density}
\alias{triage_plot_density}
\title{Triage Plot: Overall Distribution, Split by Unit}
\usage{
triage_plot_density(
  data,
  label = NULL,
  limits = NULL,
  thresholds = NULL,
  grp = NULL
)
}
\arguments{
\item{data}{A group subset, retaining all units.}

\item{label}{Group label for the subtitle.}

\item{limits}{Shared value-axis limits from \code{\link[=triage_limits_for]{triage_limits_for()}}.}

\item{thresholds}{The \code{copper_toxicity_thresholds} target, or \code{NULL} for no
reference lines.}

\item{grp}{The one-row group tibble, needed to match thresholds. Because this
panel spans units, thresholds are matched \strong{once per unit present} and the
unit is prepended to each label; matching on the group's own unit alone
would draw wet-weight lines across a dry-weight curve.}
}
\value{
A ggplot.
}
\description{
Unlike the other four views this one is deliberately \strong{unit-agnostic}: it
should be passed a subset that still contains every unit for the group (see
the \code{exclude_cols} argument of \code{\link[=filter_to_group]{filter_to_group()}}). The whole point of the
plot is to show how far dry and wet weight concentrations diverge, and since
the group key includes the unit, filtering by it would collapse exactly the
comparison the plot exists to make.
}
