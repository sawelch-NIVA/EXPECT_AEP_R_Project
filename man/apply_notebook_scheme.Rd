% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_decisions.R
\name{apply_notebook_scheme}
\alias{apply_notebook_scheme}
\title{Apply the Notebook Scheme to the Decisions File}
\usage{
apply_notebook_scheme(
  path = here::here("data/clean/group_decisions.csv"),
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{path}{The decisions CSV.}

\item{overwrite}{Replace non-empty notebook assignments too?}

\item{verbose}{Report what changed?}
}
\value{
The written tibble, invisibly.
}
\description{
Fills the \code{notebook} column mechanically from \code{\link[=notebook_scheme]{notebook_scheme()}}. It does not
touch \code{decision}, \code{lump_into} or \code{notes}: those are judgement, and the notebook
assignment is a filing question.
}
\details{
\strong{Only fills blank \code{notebook} cells} unless \code{overwrite = TRUE}, so running it
after hand-editing cannot destroy an override.

An earlier version keyed "should I fill this row?" on \code{decision} alone, and so
overwrote the \code{lump_into} of a row that had a note but no decision. It now keys
on the column it is actually writing.
}
