% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_threshold_match.R
\name{threshold_matrix_label}
\alias{threshold_matrix_label}
\title{Matrix a Threshold Was Set For}
\usage{
threshold_matrix_label(thresholds)
}
\arguments{
\item{thresholds}{Rows from a standardised threshold table.}
}
\value{
A character vector of matrix names, one per row.
}
\description{
Names the matrix each threshold applies to, for the secondary axis title.
}
\details{
The match is deliberately loose (see the notes at the top of this file):
\code{threshold_compartment_map()} sends several of our sub-compartments to one
threshold matrix, and biota match at genus. So a line drawn on a panel is
often a \emph{borrowed} comparator, and which matrix it was actually set for is
not recoverable from the class numeral alone. Sam, 2026-08-04: "though in
most cases the matrix is obvious, it isn't always."

The fraction is included for external thresholds because the Norwegian
classification boundaries are set on \strong{dissolved} copper while much of the
measured data is total, which is a real comparability caveat and is otherwise
invisible on the panel. Biota thresholds carry species and tissue instead,
which is the same information in the form biota needs it.

Columns are read defensively: \code{\link[=empty_threshold_match]{empty_threshold_match()}} does not carry
\code{ENVIRON_COMPARTMENT_SUB} or \code{THRESHOLD_FRACTION}, and callers may hand over
a subset.
}
