% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_outlier_report.R
\name{outlier_dotplot_binwidth}
\alias{outlier_dotplot_binwidth}
\title{Dotplot Binwidth Scaled to a Group's Own Data Range}
\usage{
outlier_dotplot_binwidth(x, bins = 40, min_binwidth = 0.005)
}
\arguments{
\item{x}{Numeric vector of (positive) values.}

\item{bins}{Target number of bins across the data's own range.}

\item{min_binwidth}{Floor on the returned binwidth (log10 units), so
near-identical values don't collapse to a zero-width bin.}
}
\value{
A single numeric value, for use as \code{geom_dotplot(binwidth = )}.
}
\description{
A fixed \code{binwidth} tuned for one dataset looks wildly oversized on a
small, narrow-range group and undersized on a huge, wide-range one.
Scaling \code{binwidth} to roughly \code{bins} bins across the data's own log10
range keeps dot size sensible ("one dot, one sample") regardless of the
group's scale.
}
