% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{split_triage_data}
\alias{split_triage_data}
\title{Split Data into One Slice per Group}
\usage{
split_triage_data(data, groups)
}
\arguments{
\item{data}{The \code{literature_analysis_ready} target.}

\item{groups}{Output of \code{\link[=sample_triage_groups]{sample_triage_groups()}}.}
}
\value{
A list with one element per group, each \code{list(grp = , data = )}.
}
\description{
Built once, in one target, and branched over with \code{iteration = "list"}. Doing
the slicing in a \emph{branched} target instead would send the full table to every
branch, which is the cost this is meant to remove.
}
\details{
Each element carries its own group row, so nothing downstream has to zip two
targets together with \code{map()}. That matters: \code{tar_group_by()} orders branches
by the grouping column while \code{sort_triage_groups()} orders the groups table
hierarchically, and the two need not agree. Pairing a group with another
group's slice would yield empty panels rather than an error.
}
