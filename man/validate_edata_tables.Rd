% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_validate_pointblank.R
\name{validate_edata_tables}
\alias{validate_edata_tables}
\alias{pb_validate_campaign}
\alias{pb_validate_reference}
\alias{pb_validate_parameters}
\alias{pb_validate_sites}
\alias{pb_validate_samples}
\alias{pb_validate_biota}
\alias{pb_validate_measurements}
\alias{pb_validate_methods}
\alias{pb_validate_creed_scores}
\title{Validate eData tables using pointblank}
\usage{
pb_validate_campaign(data, actions = action_levels(), agent = TRUE)

pb_validate_reference(data, actions = action_levels(), agent = TRUE)

pb_validate_parameters(data, actions = action_levels(), agent = TRUE)

pb_validate_sites(
  data,
  actions = action_levels(),
  agent = TRUE,
  northern_hemisphere = FALSE
)

pb_validate_samples(data, actions = action_levels(), agent = TRUE)

pb_validate_biota(data, actions = action_levels(), agent = TRUE)

pb_validate_measurements(data, actions = action_levels(), agent = TRUE)

pb_validate_methods(data, actions = action_levels(), agent = TRUE)

pb_validate_creed_scores(data, actions = action_levels(), agent = TRUE)
}
\arguments{
\item{data}{Data frame to validate}

\item{actions}{Action levels for pointblank agent (default: action_levels()).
Only used when agent = TRUE.}

\item{agent}{Logical. If TRUE (default), returns a pointblank agent object.
If FALSE, returns the validated data with failed rows removed.}

\item{northern_hemisphere}{Logical. If TRUE, constrains latitude validation
to northern hemisphere (0-90). Default is FALSE (allows -90 to 90).}
}
\value{
If agent = TRUE, a pointblank agent object with interrogation results.
If agent = FALSE, the input data with validation failures removed.
}
\description{
Comprehensive validation of eData format tables using pointblank.
Each validation function can operate in two modes:
\itemize{
\item Agent mode (agent = TRUE): Creates an agent with validation rules for reporting
\item Pipeline mode (agent = FALSE): Returns validated data with failures removed
}
}
