% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_aep_manifest.R
\name{aep_diagram_squeeze}
\alias{aep_diagram_squeeze}
\title{Squeeze Compensation for a Diagram That May Carry a Locator Inset}
\usage{
aep_diagram_squeeze(draw_inset, width, height, image_size, inset_width)
}
\arguments{
\item{draw_inset}{Will an inset actually be drawn for this AEP? (Already
resolved by the caller: has a bounding box AND a \code{bbox_map} was supplied.)}

\item{width, height}{Nominal canvas size in inches.}

\item{image_size}{Card width as a fraction of panel width, before
compensation.}

\item{inset_width}{Width of the locator inset relative to the main panel.}
}
\value{
A list: \code{image_size} (compensated), \code{device_aspect} (the TRUE
effective width over height, for \code{\link[=plot_aep]{plot_aep()}}) and \code{effective_width}
(inches, for \code{\link[=aep_diagram_height]{aep_diagram_height()}} -- computing that from `device_aspect
\itemize{
\item height` again at the call site would just re-derive this number by a
different route).
}
}
\description{
An AEP with a bounding box gets \code{+ inset + patchwork::plot_layout(widths = c(1, inset_width))} appended in \code{\link[=write_aep_diagrams]{write_aep_diagrams()}}, which squeezes the
diagram's own panel down to \code{1 / (1 + inset_width)} of the canvas width.
\code{ggimage::geom_image()} sizes a card as a fraction of WHATEVER panel it
actually lands in, so calling \code{plot_aep()} with the nominal canvas width
before knowing an inset is coming undersizes every card on an inset AEP
relative to a plain one -- which is exactly what Sam noticed 2026-08-08
comparing A001 (no bounding box) against A002 (Repparfjorden, boxed):
"AEPs 1 and 2 use different size rectangles."
}
\details{
Pulled out as its own function, pure arithmetic with no plotting or file
I/O, so the compensation itself can be tested directly rather than only by
rendering and measuring pixels.
}
