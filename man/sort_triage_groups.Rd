% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_group_triage.R
\name{sort_triage_groups}
\alias{sort_triage_groups}
\title{Sort Groups for Hierarchical Presentation}
\usage{
sort_triage_groups(groups)
}
\arguments{
\item{groups}{Output of \code{\link[=sample_triage_groups]{sample_triage_groups()}} before sorting.}
}
\value{
The same tibble, reordered, with \code{n_heading} added.
}
\description{
Two requirements pull against each other here: nested headings need the tree
traversed in order, while a triage sheet should lead with the groups carrying
the most data. The resolution is to nest, and order \strong{siblings within each
parent} by descending measurement count. So the heaviest compartment comes
first, and within it the heaviest sub-compartment, and so on down to the leaf.
}
\details{
Weights are \code{sum(MEASURED_N)} summed across unit variants but nothing else,
per the 2026-07-30 decision: a group split only by dry and wet weight is one
group for ordering purposes.
}
