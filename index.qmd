---
title: Copper Pollution in the Arctic - An Aggregate Exposure Pathway for Understanding Sources, Sinks, and Bioavailability
# Two outputs, added 2026-07-29: html for your own review, docx for sharing
# with co-authors. Declared here rather than in _quarto.yml so that the docx
# is only produced for the manuscript, not for every notebook in the project.
format:
  html: 
    lightbox: true
  docx:
    reference-doc: custom-reference-doc.docx
    fig-width: 8
    fig-height: 6
    fig-dpi: 300
    fig-format: png
    link-citations: true
    link-bibliography: true
---

```{r}
#| label: setup
#| include: false
#| warning: false

library(targets)
library(here)
library(tidyverse)
library(flextable)
library(sf)
library(patchwork)
library(rosm) # osm.image() Kartverket basemap tiles
library(ggspatial) # annotation_scale(), annotation_north_arrow()
pkgload::load_all(quiet = TRUE) # sample_groups_flextable(), summarise_groups() etc.

here::i_am("index.qmd")

# Shared docx table styling. theme/bold/fontsize/padding match the
# sample_groups_flextable() and node_report_flextable() helpers so every table
# in the document reads the same.
#
# `widths = NULL` (default): `layout = "autofit"` + `width = 1`, i.e. the table
# fills 100% of the text column and Word distributes the columns. Good for the
# narrow data tables.
#
# `widths = c(...)` (inches, sum to the text width -- 6.27in portrait A4 with
# 1in margins, 9.69in landscape): `layout = "fixed"`, i.e. Word uses these
# column widths exactly and wraps long cell text rather than stealing space.
# Needed for the wide reference tables where a couple of columns are prose.
docx_flex <- function(ft, widths = NULL) {
  ft <- ft |>
    flextable::theme_vanilla() |>
    flextable::bold(part = "header") |>
    flextable::fontsize(size = 9, part = "all") |>
    flextable::padding(padding = 2, part = "all")
  if (is.null(widths)) {
    flextable::set_table_properties(ft, layout = "autofit", width = 1)
  } else {
    flextable::set_table_properties(
      flextable::width(ft, width = widths),
      layout = "fixed"
    )
  }
}
```

```{=html}
<!-- Manuscript body is split into one file per top-level section and spliced
back together here at render time. Edit prose in the _NN-*.qmd files; this
file keeps only the frontmatter, the shared `setup` chunk, and the section
order. The leading underscore keeps Quarto's project render from treating the
section files as standalone documents. -->
```

{{< include _01-introduction.qmd >}}

{{< include _02-methods.qmd >}}

{{< include _03-results.qmd >}}

{{< include _04-discussion.qmd >}}

{{< include _05-conclusion.qmd >}}

# References