.Rproj.user
.Rhistory
.RData
.rdx
.Ruserdata
.Renviron
/.quarto/
**/*.quarto_ipynb
/_targets/*
/_manuscript/*
/_site/*
*.html
_freeze/*
index_files/*
docs/*_cache
docs/*_files/*
.images_raw
.claude
.vscode

# big data files
*.shp
*.zip
*.pdf
*.gpgk

# Quarto detritus
*.docx
.vdoc.*
site_libs/*
/.luarc.json

# Triage plot PNGs (regenerated by the pipeline, PLAN.md Phase 1)
triage/

# Scratch renders for looking at a change: before/after pairs, variant sweeps,
# anything drawn to be eyeballed once and thrown away. Inside the project so
# Sam can open them, out of git because they are not artefacts. See CLAUDE.md
# 2.3.1.
figures/dev/

# Build logs from hand-run pipeline invocations (see CLAUDE.md 2.3.1: Sam
# cannot see anything written to a scratch directory).
logs/

# rosm/ggspatial tile cache (annotation_map_tile downloads), not an artefact
rosm.cache/
