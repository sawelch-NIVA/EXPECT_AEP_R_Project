project:
  type: manuscript
  output-dir: _manuscript

manuscript:
  article: index.qmd

format:
  html:
    code-fold: true
    fig-format: svg
    toc: true
    comments:
      hypothesis: true
  # docx: default
  # jats: default

  # (other formats)
  # pdf: default

execute:
  freeze: true

engine: knitr

